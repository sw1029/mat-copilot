# 평가 기준 도출 및 PRD/TRD 미비점 분석 보고서

| 항목 | 내용 |
| --- | --- |
| 문서 목적 | ref.md(타 레포 심사 결과)에서 실제 심사 기준을 역산하고, 그 기준으로 현재 PRD/TRD/SCHEMA의 미비점을 분석 |
| 분석 대상 | [PRD/back.md](./PRD/back.md) v0.3, [PRD/front.md](./PRD/front.md) v0.1, [TRD/back.md](./TRD/back.md) v0.2, [TRD/front.md](./TRD/front.md), [SCHEMA/schema.md](./SCHEMA/schema.md) v0.1, 루트 `PRD.md`/`TRD.md`/`README.md` |
| 기준 출처 | [ref.md](./ref.md) (타 레포 7개 항목 심사평), [regulations.md](./regulations.md) (가중치·체크리스트) |
| 작성일 | 2026-08-22 |

---

## 1. 요약 (Executive Summary)

ref.md의 심사평을 역산하면 심사자는 **"문서의 주장 ↔ 실제 구현의 일치"**, **"프롬프트 약속의 코드 수준 강제"**, **"미사용 구현(데드 코드) 감점"**, **"운영 성숙도(IaC/헬스체크/관측성)"**, **"모든 AI 표면의 고지 일관성"** 을 점수를 가르는 결정 요소로 사용한다.

이 기준으로 볼 때 현재 문서의 **최대 리스크는 개별 문서의 품질이 아니라 문서 간 정합성과 제출 형식**이다:

1. **[P0] 제출 필수 파일인 루트 `PRD.md`·`TRD.md`가 빈 파일** — regulations.md상 문서 미비는 전 항목 평가의 근거 상실로 직결.
2. **[P0] 프론트 PRD와 백엔드 PRD가 서로 다른 제품을 기술** (백: 기획안↔결과물 drift 분석 / 프론트: 인터뷰→PRD·TRD 생성→두 문서 비교+토큰 효율) — "문서와 구현 일치" 기준에서 치명적.
3. **[P0] 마일스톤 어긋남** — 프론트 M1은 인터뷰 MVP인데 백엔드는 인터뷰(FR-2~4)를 M2로 배정. 해커톤은 단일 제출 시점이므로 "합동 MVP" 정의가 없으면 데모가 성립하지 않음.
4. **[P0] 가중치 25% 항목(Copilot SDK×MAF)의 핵심 결합부가 전부 보류** — 언어/런타임(OQ-06), SDK↔MAF 어댑터·모델(OQ-18) 미확정. 설계는 정교하나 "실제 연결" 증빙 계획이 없음.
5. **[P1] ref에서 감점된 요소들이 현재 문서에도 그대로 존재** — 헬스 엔드포인트 부재, LLM 폴백 부재(전체 차단 명시), 레이트리밋·보존정책·입력 길이 검증 보류, IaC/CI-CD 미정, 취소 기능 부재, 전후 효과 정량 주장 부재.

---

## 2. ref.md에서 도출한 평가 기준

ref.md는 7개 항목 각각에 대해 "무엇이 그 점수를 만들었고, 무엇이 다음 점수를 막았는지"를 서술한다. 이를 **가점 신호(해당 점수 도달 근거)** 와 **감점 신호(상위 점수 차단 요소)** 로 분해해 체크 가능한 기준으로 일반화했다.

### 2.1 Copilot SDK & MAF 활용 (ref 4/5 · 가중치 25%)

| 구분 | ref에서 확인된 신호 | 일반화된 기준 |
| --- | --- | --- |
| 가점 | SDK가 **유일한** 모델 연결 계층 / MAF가 핵심 기능의 **실제 실행 계층** / 근거 기반 프롬프트, 함수 도구, 구조화 JSON 검증, ID 검증, 지연 초기화, 호출 타임아웃, 규칙 기반 폴백, 생성 출처 표기 | C1-a. 두 기술이 핵심 사용자 흐름에 실연결(장식 아님)<br>C1-b. 도구 호출·구조화 출력·검증·신뢰성 장치(타임아웃/폴백)의 실재 |
| 감점 (5점 차단) | 에이전트 간 협업 부재 / 장기 대화 문맥 부재(단일 요청 태그 국한) / **구현했지만 프론트가 안 쓰는 SSE**(미사용 구현) / **프롬프트에 적힌 허용 태그를 코드에서 강제 검증하지 않음** | C1-c. 다중 에이전트 협업 + 세션 수준 문맥 처리<br>C1-d. 미사용 구현(데드 코드) 금지 — 만든 것은 반드시 사용<br>C1-e. 프롬프트가 약속한 제약은 결정적 코드로 검증 |

### 2.2 생산성 효과 및 문제 적합성 (ref 4/5 · 18%)

| 구분 | ref 신호 | 일반화된 기준 |
| --- | --- | --- |
| 가점 | 명확한 대상 사용자 / 연속된 생산성 문제 정의 / **구체적 전후 효과(10분→약 1분)** + 구현이 주장을 뒷받침 | C2-a. 구체 페르소나·문제<br>C2-b. 수치화된 전후 비교가 문서에 존재하고 구현으로 재현 가능 |
| 감점 (5점 차단) | 성공 기준(90초)의 **실측·사용자 검증 부재** / 심사자가 결과 화면까지 **완주하지 못함** / **화면 문구와 실제 동작 불일치**(1인분 구매 vs 판매 팩 단위) | C2-c. 주장 수치는 실측 또는 검증 근거 동반<br>C2-d. 심사자가 로그인 없이 결과까지 완주 가능해야 함(데모 데이터 포함)<br>C2-e. UI 문구·문서 주장 = 실제 동작 |

### 2.3 Azure 클라우드 통합 (ref 3/5 · 18%)

| 구분 | ref 신호 | 일반화된 기준 |
| --- | --- | --- |
| 가점 (3점 = "배포됨") | 실제 배포 확인 / 규모에 맞는 구조 / 시크릿 참조 / 장애 폴백 | C3-a. 실배포 + 규모 적합 구성이 3점의 하한 |
| 감점 (4점 차단) | 수동 `az` 배포 / **IaC(Bicep/Terraform)·CI/CD 부재** / **관리 ID·Key Vault·관측성·헬스 프로브·복제/자동확장·DR 부재** / ACR 관리자 인증 활성화(보안 안티패턴) | C3-b. 재현 가능한 배포(IaC 또는 최소 CI/CD)<br>C3-c. 관리 ID/Key Vault 기반 시크릿<br>C3-d. 헬스 프로브·관측성·확장 설정 등 운영 성숙도 |

### 2.4 기능 완성도 및 기술 구현 (ref 4/5 · 16%)

| 구분 | ref 신호 | 일반화된 기준 |
| --- | --- | --- |
| 가점 | 배포 앱 정상 실행·무로그인 진입 / 계층 분리 / 타임아웃+폴백+검증의 신뢰성 설계 | C4-a. E2E 완주 동작 + 명확한 계층 분리 |
| 감점 (5점 차단) | **예외를 기록 없이 삼킴** / **자동화 테스트 부재** / **상태 확인(헬스) 엔드포인트 부재** / 스트리밍 경로 타임아웃 부재 / 프론트 일부 요청의 HTTP 오류 미확인 | C4-b. 모든 예외 기록(silent catch 금지)<br>C4-c. 자동화 테스트 실재<br>C4-d. 헬스 엔드포인트<br>C4-e. 모든 외부 호출에 타임아웃 + 프론트 전 요청 오류 확인 |

### 2.5 UX 및 워크플로 설계 (ref 4/5 · 12%)

| 구분 | ref 신호 | 일반화된 기준 |
| --- | --- | --- |
| 가점 | 무로그인 원클릭 진입 / 진행 표시·이전/재시작 / AI 역할 화면 설명·생성 대기 문구 / 모바일 우선 | C5-a. 즉시 진입 + 흐름 연결 + AI 역할 설명 |
| 감점 (5점 차단) | 일부 요청에 **사용자용 오류 상태·재시도 부재** / **최대 45~90초 호출에 취소 기능 부재** / 동적 상태 알림·진행률·입력·체크박스의 **접근성 이름·포커스 관리 부족** | C5-b. 전 요청 오류 상태 + 재시도<br>C5-c. 장시간 작업 취소(사용자 통제권)<br>C5-d. aria-live·접근성 이름·포커스 관리의 구체 구현 |

### 2.6 책임 있는 AI·보안·신뢰 (ref 3/5 · 6%)

| 구분 | ref 신호 | 일반화된 기준 |
| --- | --- | --- |
| 가점 | AI 생성·추정 성격 고지 / 폴백 / 도구·ID를 내부 카탈로그로 제한 / 하드코딩 시크릿 없음 | C6-a. 고지 + 행동반경 제한 + 시크릿 관리가 3점 하한 |
| 감점 (4점 차단) | Key Vault/관리 ID 부재 / **프롬프트 인젝션 명시 탐지·차단 부재** / **입력 길이·허용값 검증 부재** / **호출 제한(rate limit) 부재** / **보존·삭제 정책 부재** / 보안 헤더 부재 / **일부 표면(채팅 UI)의 AI 고지 누락** / 모델 출력 태그의 allowlist 미검증 | C6-b. **모든** AI 표면에서 고지 일관성<br>C6-c. 입력 검증·rate limit·보존 정책·보안 헤더<br>C6-d. 모델 출력의 코드 수준 allowlist 검증 |

### 2.7 혁신성·독창성 (ref 4/5 · 5%)

| 구분 | ref 신호 | 일반화된 기준 |
| --- | --- | --- |
| 가점 | 단순 챗봇 복제가 아닌 문제 맞춤 의사결정 흐름 / 생성·도구 조합의 역할 분리 설계 | C7-a. 문제에 맞는 차별화된 AI 메커니즘 |
| 감점 (5점 차단) | 기반 엔진이 단순(18개 정적 카탈로그 + 단순 태그 점수) | C7-b. 데모에서 체감되는 기반 메커니즘의 깊이 |

---

## 3. 현재 문서 현황

| 문서 | 상태 | 비고 |
| --- | --- | --- |
| 루트 `PRD.md` | **빈 파일 (0 byte)** | regulations §1 제출 필수 문서 |
| 루트 `TRD.md` | **빈 파일 (0 byte)** | regulations §1 제출 필수 문서 |
| 루트 `README.md` | 빈 파일 | 보조 문서 |
| `PRD/back.md` | v0.3, 상세 작성됨 | FR 11종·NG 6종·리스크·마일스톤 포함 |
| `PRD/front.md` | v0.1, 상세 작성됨 | 단, 백엔드 PRD와 제품 정의가 다름 (§5.2 참조) |
| `TRD/back.md` | v0.2, 594줄 상세 작성됨 | 단, 헤더가 선언한 §13.2·부록 B가 **본문에 없음** |
| `TRD/front.md` | **빈 파일 (0 byte)** | schema·프론트 PRD가 위임한 항목의 수신처 부재 |
| `SCHEMA/schema.md` | v0.1, API 14종·모델·오류 정의 | 존재하지 않는 TRD §13.2/부록 B/OQ-21/OQ-22 참조 |

---

## 4. 기준별 미비점 상세 분석

각 항목은 `[심각도] 미비점 — 근거 — 개선 방향` 형식이다. 심각도: **P0**(탈락·최저점 또는 최상위 가중치 직결) / **P1**(명확한 감점 요인) / **P2**(상위 점수 차단 요인).

### 4.1 Copilot SDK & MAF 활용 (25%) — 기준 C1

**[P0] 핵심 결합부(SDK↔MAF 어댑터·모델·언어)가 전부 보류 상태**
- 근거: TRD/back.md §3.2 — 언어/런타임·웹 프레임워크 보류(OQ-06), §5.2 — "어댑터 구현 세부와 모델 선택은 보류(OQ-18)". MAF 공식 지원(Python/.NET)과 Copilot SDK 지원 언어의 **호환 매트릭스 확인이 계획으로만 존재**.
- 평가 관점: ref는 "실제 연결되어 있어 강함"이 4점의 근거였다. 가장 높은 가중치 항목의 실현 가능성이 문서상 미검증이면, 구현 단계에서 이 부분이 무너질 경우 대체 경로가 없다.
- 개선: OQ-06/OQ-18을 최우선 스파이크로 확정하고, TRD에 (1) 선택 언어·프레임워크, (2) Copilot SDK를 MAF chat client로 주입하는 코드 수준 방식, (3) 사용 모델 ID·구조화 출력 방식·agent별 파라미터를 명기. regulations TRD 체크리스트("모델 연결 방식")의 직접 요건.

**[P0] MAF 활용의 정수(인터뷰 reflection 루프)가 백엔드 M1에서 제외됨**
- 근거: TRD/back.md §1.2 — M1은 FR-1/5/6/7(파일)/8(1종)/10/11만 포함, 인터뷰(FR-2~4, AG-02/03/04)는 M2. 반면 PRD/front.md §13 M1은 "인터뷰 MVP"가 최우선.
- 평가 관점: ref가 5점을 못 받은 이유가 "에이전트 간 협업·장기 문맥 부재"였다. 현 M1 파이프라인(추출→정규화→평가→drift 1종→보고서)은 순차 처리 위주로, 협업이 fan-out 1종 + 검증(AG-12)에 그침. 차별화 요소인 생성↔검증 reflection 루프·다중 테마 동시 실행이 데모에 없으면 25% 항목에서 ref 대비 우위를 잃는다.
- 개선: "합동 MVP" 재정의 시 인터뷰 루프 최소형(깊이 1~2, AG-02+AG-03 이중화)을 M1에 포함하거나, 최소한 DRIFT 코어 2종 이상 동시 fan-out을 M1로 승격해 "협업하는 다중 에이전트"를 데모 가능하게 할 것.

**[P1] LLM 장애 시 대체 경로 부재 — "전체 차단"을 명문화**
- 근거: TRD/back.md §1.3 ASM-03 — "불가 시 전체 차단 → LLM_UPSTREAM_ERROR 처리". §11.2는 재시도(2회)만 규정.
- 평가 관점: ref는 "규칙 기반 폴백"이 SDK/MAF 항목과 기능 완성도 항목 양쪽에서 가점 근거였다. 현재 설계는 심사 시점에 모델 장애가 나면 데모 전체가 불능이 된다.
- 개선: 데모 안전장치(예: 사전 계산된 샘플 세션/보고서 폴백, 인터뷰 규칙 기반 기본 질문 세트)를 TRD에 명시. "AI 불가 시에도 흐름은 완주 가능"을 보장.

**[P1] agent별 호출 타임아웃·턴당 지연 예산 미정의**
- 근거: SCHEMA §1은 클라이언트 권장 타임아웃(인터뷰 턴 60s)만 규정. 한 턴에 AG-03 산출→가드→AG-02 생성→AG-03 재검증까지 **직렬 LLM 호출 3회 이상**이 동기 HTTP 안에서 수행되는데(TRD §6.1), 서버 측 LLM 호출별 타임아웃·턴당 예산·후보 질문 수 상한이 없음.
- 평가 관점: ref는 "호출 타임아웃"을 가점, "스트리밍 경로 타임아웃 부재"를 감점 요소로 언급. 60초 초과 시 프론트 타임아웃과 서버 진행이 충돌한다.
- 개선: LLM 호출별 타임아웃(예: 15s)·턴당 총예산(예: 45s)·생성 후보 수 상한을 TRD §6.1/§11.2에 확정.

**[P2] 세션 문맥(컨텍스트 패킷)의 토큰 예산 전략 부재**
- 근거: TRD §5.1은 컨텍스트 패킷 조립을 선언했으나, 질문 트리·기획안·결과물 발췌가 커질 때의 요약/절단 정책이 없음.
- 개선: 패킷 구성요소별 토큰 상한과 요약 규칙을 명시. ref의 "장기 대화 문맥" 감점을 넘어서는 차별점으로 활용 가능.

### 4.2 생산성 효과·문제 적합성 (18%) — 기준 C2

**[P1] 전후 비교 수치 주장이 전무 — regulations 체크리스트 직접 위반**
- 근거: PRD/back.md §8 성공 지표 4종 전부 "베이스라인 측정 후 설정", PRD/front.md §10도 전부 "측정 필요". "도입 전 수동 검토 N분 → 도입 후 M분" 류의 주장 자체가 없음.
- 평가 관점: ref는 미실측이어도 "10분→1분"이라는 구체 주장+구현 뒷받침으로 4점을 받았다. 주장 자체가 없으면 regulations §2 "도입 전후의 시간·단계·품질 차이 입증" 요건에서 4점 근거를 만들 수 없다.
- 개선: PRD에 대표 시나리오 기반 전후 비교를 수치로 명시(예: "AI 위임 산출물의 의도 어긋남 수동 검토 30분+누락 위험 → 업로드 후 N분 내 근거 인용 포함 보고서"). 가능하면 자체 사용 실측 1회라도 기록.

**[P0] 백/프론트 PRD가 서로 다른 대상 문제·제품을 정의**
- 근거: PRD/back.md §1 — "기획안·인터뷰로 의도 확보 → 제출된 **결과물**(문서/코드/링크)의 drift 분석". PRD/front.md §1 — "인터뷰 → **PRD·TRD 생성** → 두 문서 간 왜곡·누락·**토큰 효율성** 분석". 페르소나도 상이(백: 즉흥 작업자 일반 / 프론트: 기획자·PM, 개발자, AI 도구 사용자). TRD/back.md OQ-17이 이 불일치를 인지했으나 "합의 필요"로 보류.
- 평가 관점: 심사자는 PRD를 근거로 구현 일치를 본다(C2-e). 두 PRD가 다른 제품을 말하면 어느 쪽으로 구현해도 문서-구현 불일치가 발생한다.
- 개선: OQ-17을 즉시 종결하고 단일 제품 서사로 루트 PRD.md를 작성. 프론트의 "PRD/TRD 생성·비교"를 채택한다면 백엔드 FR·JobStage·데이터 모델의 대개편이 필요하므로, 범위 판단을 지금 해야 함.

**[P1] 심사자 완주 경로(데모 시나리오·데모 데이터) 미문서화**
- 근거: 두 PRD 모두 게스트 진입(무로그인)은 다루나, regulations §3 "게스트 또는 데모 경로 설명"·§4 "게스트 또는 데모 데이터 준비"에 해당하는 **구체 시나리오(샘플 기획안 파일, 샘플 결과물, 예상 소요시간, 완주 절차)** 가 없음. ref는 "스카우트가 결과 화면까지 완주하지 못함"이 감점 사유였다.
- 개선: PRD에 "심사용 3분 데모 경로" 절 신설 — 내장 샘플 기획안/결과물 원클릭 로드, 각 단계 예상 대기시간, 실패 시 우회 경로.

### 4.3 Azure 클라우드 통합 (18%) — 기준 C3

**[P1] Azure 구성 전체가 단일 보류(OQ-14)에 묶여 있음**
- 근거: TRD/back.md §3.2·§8.2·§12.1 — 호스팅(App Service vs Container Apps), 저장소, 시크릿 주입(환경변수 vs Key Vault), 로그 sink, CI/CD, 프론트 결합 방식 전부 보류.
- 평가 관점: regulations TRD 체크리스트 "사용 중인 Azure 서비스와 이유", "재현 가능한 배포 절차"가 현재 충족 불가. ref 기준 3점(배포됨)은 구현으로 달성 가능하나, 문서상 근거가 없으면 3점 방어도 어렵다.
- 개선: OQ-14를 분해해 즉시 확정 — 최소 구성 권고: Container Apps(또는 App Service) + Blob(파일·보고서) + 앱 설정/Key Vault + Application Insights. "필요 이상 서비스 금지" 원칙과 정합하게 각 서비스의 필요 이유를 1줄씩 명기.

**[P1] 헬스 프로브 엔드포인트가 API 목록에 없음 — ref 명시 감점 항목 재현**
- 근거: SCHEMA §2 API-01~14에 `/health`류 부재. ref: "상태 확인 엔드포인트가 없으며"(기능 완성도 감점), "헬스 프로브 … 확인되지 않고"(Azure 감점).
- 개선: `GET /health`(liveness)·`GET /ready`(LLM 연결 포함 readiness)를 SCHEMA에 추가하고 호스팅 프로브에 연결.

**[P2] IaC·CI/CD 부재 — 4점 차단 요소 그대로 재현 예정**
- 근거: TRD §12.1 "CI/CD: GitHub Actions 후보 — 보류". IaC는 언급 자체가 없음. ref 4점 차단 사유: "수동 az 명령 의존, Bicep/Terraform·CI/CD 없음".
- 개선: 해커톤 시간 내 최소형으로 (1) `azd`/Bicep 단일 템플릿 또는 (2) GitHub Actions 배포 워크플로 중 하나를 M1 범위에 넣고 TRD에 절차를 명기.

**[P2] 저장소 후보 (a) 인메모리와 무상태·멀티테넌시 주장의 내부 모순**
- 근거: TRD §8.2 후보 (a) "인스턴스 재시작 시 세션 유실, 수평 확장 불가" ↔ §8.3 "API 계층 무상태", G8 멀티테넌시, 프론트 FR-10 "새로고침 후 복구". 재시작 시 프론트의 복구 보장이 깨짐. ref도 인메모리 특성이 "복제본/자동 확장 부재"와 함께 언급됨.
- 개선: (a) 선택 시 제약(단일 인스턴스, 재시작 유실)을 PRD 제약 절과 프론트 문서에 전파하거나, 세션·job 상태만 외부화(Table/Cosmos)하는 절충안을 확정.

### 4.4 기능 완성도·기술 구현 (16%) — 기준 C4

**[P0] 프론트 데이터 계약이 SCHEMA에 존재하지 않음 — 결과 화면 구현 불가 상태**
- 근거: PRD/front.md §8.1 요구 필드(`documentId/markdown/downloadUrl`, `comparisonId/footnoteNumber/prdRange/trdRange`, `metricId/threshold/status`, 토큰 사용량) ↔ SCHEMA §4에는 `Report/Finding/EvidenceRef/ChartSpec`만 존재. 프론트 §5.3 상태 `GENERATING_PRD/GENERATING_TRD`는 SCHEMA `JobStage`(INGEST→…→REPORT)에 없음. 문서 다운로드(프론트 FR-18)·토큰 지표(프론트 G7) 엔드포인트 부재. SCHEMA §7.1이 일부를 "확장 후보"로만 나열.
- 평가 관점: 이대로 구현하면 프론트 결과 화면(각주·교차 강조·지표)이 통째로 미동작하거나 문서와 불일치 — C4-a(E2E 완주)와 C2-e(주장=동작) 동시 실패.
- 개선: OQ-17 종결과 함께 SCHEMA를 단일 제품 기준으로 개정. 프론트 §8.1 표의 필드 하나하나에 대응하는 SCHEMA 모델/엔드포인트를 명기하거나, 프론트 PRD에서 해당 기능을 명시적으로 축소.

**[P1] 테스트 전략에 실행 수단이 없음**
- 근거: TRD §14는 TC-01~13을 정의(ref 대비 우수)하나, 테스트 프레임워크·실행 명령·CI 연동이 전부 OQ-06(언어 미정)에 종속되어 공란. ref 감점: "자동화 테스트가 없으며".
- 개선: 스택 확정 즉시 테스트 러너·명령·CI 트리거를 TRD에 추가. 최소한 TC-12(E2E)와 TC-13(계약 테스트)은 M1 완료 게이트로 명문화되어 있으므로 실행 방법을 채울 것.

**[P1] 프론트 TRD 부재로 오류 처리 위임이 공중에 뜸**
- 근거: TRD/front.md 0 byte. SCHEMA §1 "백오프 상세는 프론트 TRD", 프론트 PRD §12 "폴링/실시간 방식과 재연결 정책을 TRD에서 확정" — 수신처가 없음. ref 감점: "일부 프론트엔드 요청도 HTTP 오류를 확인하지 않아".
- 개선: TRD/front.md 작성 — 재시도/백오프, 폴링 간격(스키마 권장 2s)·중단 조건, 상태 복구 절차, 오류 상태 매핑(SCHEMA §5 코드 → 화면 문구), 렌더링 성능 전략.

**[P2] 프로세스 재시작 시 job 복구 시나리오 미정의**
- 근거: TRD §7.8 체크포인트는 있으나, 인프로세스 job 실행기(§4.1)가 재시작하면 RUNNING job의 소유권·재개 주체가 미정(저장소 (a)면 기록도 소실).
- 개선: "RUNNING 상태로 방치된 job의 FAILED 전환 + retry 유도" 규칙을 §7.8에 추가.

**[P2] 모바일 대응 입장이 규정 점검 항목과 상충**
- 근거: PRD/front.md NG5·§9(최소 1280×720, 데스크톱 기준) ↔ regulations §4 "모바일 및 일반 데스크톱 화면에서 핵심 UI 사용 가능". ref는 모바일 우선이 가점이었다.
- 개선: 전면 대응이 어렵다면 최소한 결과 조회·보고서 열람 등 핵심 판독 경로만 모바일 가독 보장으로 범위를 정의하고 PRD에 명시(의도적 트레이드오프임을 문서화).

### 4.5 UX·워크플로 설계 (12%) — 기준 C5

**[P1] 장시간 작업 취소 수단 부재 — ref 명시 감점 재현**
- 근거: ref "최대 45~90초 호출에 대한 취소 기능도 없습니다". 현 설계: 인터뷰 턴 60s 동기 호출·분석 job 폴링 모두 취소 API 없음(SCHEMA에 job 취소 엔드포인트 부재). 유저 주도 조기 종료(OQ-12)도 보류 — SCHEMA §7은 "도입 권고"까지만.
- 개선: `POST /jobs/{id}/cancel` 또는 최소한 인터뷰 조기 종료(OQ-12) 채택을 MVP로 승격. 프론트에는 진행 중 요청 abort + 상태 정리 UX 명시.

**[P1] AI 생성 고지가 보고서 한 곳에만 존재 — 고지 일관성 결여**
- 근거: SCHEMA `Report.aiGeneratedNotice`만 존재. 그러나 **인터뷰 질문 자체(AG-02 생성)**, 추출된 의도, 개선제안 등 다른 AI 표면에 고지 필드·표시 요구가 없음. 프론트 PRD 화면 명세(§6.1~6.2 질문 카드)에도 AI 생성 표기 요구 없음. ref 감점: "채팅 UI는 `ai_generated` 값을 표시하지 않으며" — **일부 표면 누락이 정확히 감점 사유**였다.
- 개선: 질문 카드·의도 요약·개선제안 등 모든 AI 산출 표면에 고지를 프론트 PRD 화면 요구사항으로 추가하고, 필요 시 SCHEMA에 표면별 고지 필드 반영.

**[P2] 캔버스 마인드맵의 접근성 실행 방안이 선언 수준**
- 근거: 프론트 PRD §9 WCAG 2.1 AA·키보드 100%를 목표로 선언했으나, 드래그 캔버스에서의 키보드 노드 탐색 모델, 동적 노드 추가 시 aria-live 알림, 포커스 이동 규칙 등 구체 설계가 없음(TRD/front.md 공란). ref 감점: "동적 상태 알림… 접근성 이름과 포커스 관리가 부족".
- 개선: TRD/front.md에 키보드 인터랙션 모델(노드 간 이동 키, 활성 질문 포커스 규칙)과 live region 사용처를 명세.

**[P2] 인터뷰 턴 대기 UX의 완충 장치가 미채택 제안 상태**
- 근거: SCHEMA §7 "30s 경과 시 안심 문구, p50>5s 실측 시 SSE 재검토"는 **제안 상태**로 계약에 미반영. 동기 60s 턴의 체감 지연 리스크는 프론트 PRD §12에 인지만 되어 있음.
- 개선: 스켈레톤 + 경과 안내 문구를 프론트 FR로 승격(제안→확정).

### 4.6 책임 있는 AI·보안·신뢰 (6%) — 기준 C6

현 TRD의 강점: 불신 데이터 블록 격리, tool allowlist, 근거 인용 의무+결정적 substring 검증, confidence 강등, SSRF 가드, zip 안전 규칙 — **ref의 3점 요인 다수를 설계로 선제 해소**했다. 남은 미비점:

**[P1] 무로그인 공개 API에 rate limit 부재 — 비용·남용 방어 공백**
- 근거: SCHEMA §1 rate limit 보류(OQ-13, "배포 후 부하 특성 확인"). API-01 세션 발급이 무제한이면 익명 사용자가 LLM 호출 비용을 무한 유발 가능. ref 감점: "호출 제한 … 없다".
- 개선: 최소형이라도 MVP 포함 — 세션 발급 IP당 제한, 세션당 인터뷰 턴/분석 job 수 상한, 세션당 누적 토큰 상한(관측성 로그를 차단 기준으로 활용).

**[P1] 입력 필드 길이·허용값 검증 규칙 미정의**
- 근거: SCHEMA `INVALID_INPUT`은 있으나 `Answer.value` 최대 길이, settings 범위(`confuseThreshold` 0~1 외), URL 형식·호스트 allowlist(GITHUB 타입의 github.com 한정 등) 규칙이 없음. ref 감점: "입력 길이/허용값 검증 … 없다".
- 개선: SCHEMA §4 각 모델에 길이·범위 제약 열 추가, 검증 실패 시 오류 응답 예시 포함.

**[P2] 데이터 보존·삭제 정책 미정(OQ-07) + 세션 삭제 수단 부재**
- 근거: TRD §10.2 보류. 유저가 올린 기획안·결과물(민감 가능)이 언제 지워지는지 계약이 없고, 유저 주도 삭제 API도 없음. ref 감점: "민감정보 삭제·보존 정책 … 없다".
- 개선: 해커톤 기준 단순 정책 확정(예: 세션 TTL 24h + 만료 시 파기, `DELETE /sessions/{id}` 제공)을 권고. regulations TRD 체크리스트("개인정보 … 관리 방식") 직접 요건.

**[P2] 보안 헤더·응답 위생 정책 부재**
- 근거: 양 TRD에 CSP·보안 헤더 언급 없음(프론트 PRD의 XSS-safe 렌더링 선언만 존재, TRD/front.md 공란). ref 감점: "보안 헤더 구성이 없다".
- 개선: TRD에 기본 헤더 세트(CSP, X-Content-Type-Options 등)와 보고서 markdown 렌더링 시 sanitize 라이브러리 명시.

**[P2] 인젝션 '격리'는 있으나 '탐지·대응' 정책은 없음**
- 근거: TRD §10.4는 격리·allowlist 중심. 결과물 내 악의적 지시문이 발견됐을 때의 처리(무시하고 계속 vs finding으로 보고 vs 차단)가 미정의. ref 감점: "명시적 탐지·차단 … 없다".
- 개선: "데이터 블록 내 지시문 발견 시 해당 내용을 finding(참고)으로 표면화하고 지시로는 불이행"을 명문화 — 오히려 본 제품 컨셉(drift/할루시네이션 분석)과 시너지가 있는 차별화 포인트.

### 4.7 혁신성·독창성 (5%) — 기준 C7

**[P2] 혁신 요소가 전부 M2에 몰려 있어 데모 체감 깊이가 약함**
- 근거: 독창적 요소(질의 트리+confused 산식, 마인드맵, 테마 fan-out, 동적 보조 테마, 차트)는 M2. M1 데모는 "파일 업로드→텍스트 보고서"로, ref가 5점을 안 준 사유("기반 엔진 단순")가 M1에 그대로 적용될 수 있음.
- 개선: §4.1 두 번째 항목과 동일 — 합동 MVP에 인터뷰 최소형 또는 다중 테마 fan-out 중 최소 1개를 포함.

**[P2] 핵심 정량 지표(coveredIntents)의 판정 의미론 미정의**
- 근거: SCHEMA `quantStats.coveredIntents`("커버된 요구 수")가 어떤 기준으로 "커버됨"으로 판정되는지 TRD §7.4~7.6 어디에도 산정 규칙이 없음(집계는 결정적이나 판정 입력의 정의가 공백).
- 개선: "의도별 커버 판정 = 해당 의도에 REQUIREMENT_OMISSION finding이 없고 근거 인용이 1건 이상" 같은 결정적 규칙을 TRD에 명기 — 본 제품의 신뢰성 주장(개수 기반 정량)의 근간.

---

## 5. 교차 이슈 — 문서 정합성·제출 규정 (전 항목 공통 감점 위험)

본 제품이 "문서 간 drift 탐지" 서비스라는 점에서, **자기 문서의 drift는 심사 서사상 치명적**이다.

| ID | 심각도 | 이슈 | 근거 |
| --- | --- | --- | --- |
| X-1 | **P0** | 제출 필수 루트 `PRD.md`·`TRD.md`가 빈 파일. TRD/back.md 부록 A가 "종합하여 작성한다"고 선언만 함 | regulations §1 "저장소 루트에 정확한 이름으로", 루트 파일 0 byte |
| X-2 | **P0** | 프론트/백 제품 정의 불일치(OQ-17) 미해소 — JobStage·데이터 계약·페르소나·지표 전반이 상충 | §4.2, §4.4 상세 |
| X-3 | **P0** | 프론트 M1(인터뷰) ↔ 백 M1(인터뷰 제외) 마일스톤 충돌 — 단일 제출 시점에 합동 MVP 부재 | PRD/front.md §13 vs TRD/back.md §1.2 |
| X-4 | P1 | TRD/back.md v0.2 헤더가 선언한 "§13.2 미결 항목 결정 제안"·"부록 B 프론트 통합 분석"이 **본문에 존재하지 않음** | TRD/back.md:5 vs 본문(§13은 표 1개, 부록은 A뿐) |
| X-5 | P1 | SCHEMA §7이 존재하지 않는 TRD §13.2·부록 B.2/B.4/B.5·**OQ-21·OQ-22**를 참조(OQ 대장은 OQ-20까지) | SCHEMA/schema.md §7·§7.1 |
| X-6 | P1 | TRD/front.md 0 byte — 프론트 PRD §15 미결 4건, SCHEMA가 위임한 백오프/재연결 정책의 수신처 부재 | TRD/front.md, SCHEMA §1 |
| X-7 | P2 | README.md 공란 — 보조 문서이나 실행/데모 안내 부재는 심사 완주율(C2-d)에 불리 | regulations §3 |
| X-8 | P2 | 보류(OQ) 20건 중 P0급 의존(OQ-06/14/17/18)이 병목 구조인데 확정 기한·담당이 없음 | TRD/back.md §13 |

---

## 6. 우선순위 조치 목록

| 순위 | 조치 | 해소되는 미비점 | 대상 문서 |
| --- | --- | --- | --- |
| 1 | **OQ-17 종결: 단일 제품 정의 확정** (권고: 프론트의 "인터뷰→문서 생성→비교" 서사와 백의 "결과물 drift 분석" 중 택1 또는 명시적 통합) | X-2, X-3, §4.2, §4.4 | PRD 양쪽, SCHEMA |
| 2 | **OQ-06/OQ-18 스파이크: 언어+SDK↔MAF 어댑터+모델 확정** — 결과를 TRD §3/§5에 반영 | §4.1 (25% 항목) | TRD/back.md |
| 3 | **합동 MVP 재정의** — 인터뷰 최소형 포함 여부 결정, 프론트/백 M1 동기화 | X-3, §4.1, §4.7 | PRD/TRD 양쪽 |
| 4 | **루트 PRD.md·TRD.md 작성** (하위 문서 종합 + regulations §3 체크리스트 전 항목 커버) | X-1 | 루트 |
| 5 | OQ-14 분해 확정: 호스팅·저장소·시크릿·로그 sink·배포 절차(가능하면 azd/Bicep 또는 Actions) | §4.3 | TRD/back.md |
| 6 | SCHEMA 개정: `/health`·`/ready` 추가, 취소/조기 종료 채택, 입력 길이·허용값 제약, AI 고지 필드 확장, 깨진 참조(X-5) 정리 | §4.3~4.6, X-5 | SCHEMA, TRD |
| 7 | PRD에 **정량 전후 비교 주장 + 심사용 3분 데모 경로(샘플 데이터 포함)** 신설 | §4.2 | PRD 양쪽, README |
| 8 | LLM 폴백(데모 안전장치)·rate limit 최소형·보존 정책(TTL+삭제)·턴당 지연 예산 확정 | §4.1, §4.6 | TRD/back.md |
| 9 | TRD/front.md 작성(오류/백오프/복구/접근성 인터랙션 모델/보안 헤더) | X-6, §4.4~4.6 | TRD/front.md |
| 10 | coveredIntents 판정 규칙 등 지표 의미론 명기, TRD 헤더-본문 불일치(X-4) 정리 | §4.7, X-4 | TRD/back.md |

### 예상 효과 요약

- 조치 1~4는 **탈락·최저점 방지선**(regulations §1)과 가중치 상위 2개 항목(25%+18%)의 근거를 만든다.
- 조치 5~8은 ref에서 확인된 **반복 감점 패턴**(헬스체크·폴백·rate limit·보존정책·취소·고지 일관성)을 선제 제거해 Azure(18%)·기능(16%)·UX(12%)·책임 AI(6%)에서 ref 대비 우위를 확보한다.
- 조치 9~10은 5점 차단 요소(접근성 구체화, 지표 신뢰성, 문서 자기 정합성)를 겨냥한다.
