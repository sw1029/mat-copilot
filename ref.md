Effective Use of Copilot SDK & Microsoft Agent Framework
4/5
GitHub Copilot SDK가 유일한 모델 연결 계층이고 Microsoft Agent Framework가 적응형 퀴즈·도구 기반 채팅·AI 코치의 핵심 실행 계층으로 실제 연결되어 있어 ‘강함’ 수준이다. 카탈로그에 근거한 프롬프트, 함수 도구 2개, 구조화 JSON 검증, 메뉴 ID 검증, 지연 초기화, 호출 타임아웃, 규칙 기반 폴백, 생성 출처 표기까지 설계가 사려 깊다. 다만 에이전트 간 협업이나 장기 대화 문맥은 없고 문맥은 단일 요청의 태그에 국한되며, SSE 스트리밍 구현도 현재 프론트엔드가 사용하지 않는다. 또한 적응형 결과가 프롬프트에 적힌 허용 태그 집합을 코드에서 강제 검증하지 않아 5점의 정교함에는 미치지 못한다.

Productivity Impact & Problem Fit
4/5
대상 사용자가 1인 가구 대학생·사회초년생으로 명확하고, 메뉴 결정 피로·수동 재료 정리·비용 불확실성·남는 재료라는 연속된 생산성 문제에 잘 맞습니다. 기존 10분 이상의 메뉴 고민을 4~6문항 약 1분으로 줄이고, 추천부터 1인분 재료·구매비·조리 순서·다음 끼니까지 한 화면 흐름으로 연결한 전후 효과가 구체적이며 구현도 뒷받침됩니다. 다만 90초 성공 기준은 실제 측정 결과나 사용자 검증이 없고, 스카우트가 결과 화면까지 완주하지는 않아 ‘명백하고 큰 효과’가 실증된 5점보다는 설득력 있는 4점입니다. 또한 ‘딱 1인분만 구매’라는 화면 문구와 달리 실제 구매 총액은 최소 판매 팩 단위라, 재료 낭비 해소 효과는 다음 끼니 제안에 의존합니다.

Azure Cloud Integration
3/5
실제 Azure Container Apps 배포와 Azure Container Registry 기반 이미지 공급이 확인되어 ‘배포됨’ 수준에 해당합니다. 단일 컨테이너 구조는 현재 규모와 인메모리 데이터 특성에 맞고, Container Apps 시크릿 참조 및 AI 장애 시 규칙 기반 폴백도 갖췄습니다. 다만 배포가 수동 `az` 명령에 의존하며, 제출 트리에 Bicep/Terraform 같은 IaC와 CI/CD가 없습니다. 관리 ID·Key Vault·관측성·헬스 프로브·복제본/자동 확장·재해복구 설정도 확인되지 않고 ACR 관리자 인증을 활성화하므로, 보안·반복성·운영 성숙도가 요구되는 4점에는 미치지 못합니다.

Functionality & Technical Execution
4/5
배포 앱이 정상 실행되고 핵심 퀴즈 흐름에 무로그인으로 진입하며, FastAPI API·정적 SPA·규칙 기반 추천/가격 계산·AI 에이전트 계층이 명확히 분리되어 있습니다. AI 호출에 타임아웃과 규칙 기반 폴백을 두고 잘못된 메뉴를 검증하는 등 신뢰성 설계도 충실해 ‘견고함’ 수준입니다. 다만 예외를 기록 없이 삼키는 구간이 많고, 자동화 테스트와 상태 확인 엔드포인트가 없으며, 스트리밍 경로에는 명시적 타임아웃이 없습니다. 일부 프론트엔드 요청도 HTTP 오류를 확인하지 않아 최고 수준의 안정성과 관측 가능성에는 미치지 못합니다.

User Experience & Workflow Design
4/5
매끄러운 수준입니다. 로그인 없이 한 번의 클릭으로 선택형 퀴즈에 진입하고, 진행 표시·이전/재시작·자연어 채팅·대안 메뉴·보유 재료 차감 재계산까지 하나의 모바일 우선 흐름으로 잘 연결됩니다. AI가 다음 질문과 추천 이유를 만든다는 역할을 화면에서 설명하고, 생성 대기 문구와 AI 전용 시각 영역도 제공합니다. 다만 추천·메뉴 상세·비용 재계산 요청에는 사용자용 오류 상태나 재시도 수단이 없고, 최대 45~90초 호출에 대한 취소 기능도 없습니다. 또한 동적 상태 알림, 진행률, 채팅 입력, 재료 체크박스의 접근성 이름과 포커스 관리가 부족하여 ‘매우 즐거운’ 5점에는 미치지 못합니다.

Responsible AI, Security & Trust
3/5
기본 수준의 책임 있는 AI·보안·신뢰 구현이다. AI 생성 여부와 가격의 추정 성격을 화면/API에서 알리고, 모델 장애 시 규칙 기반 폴백을 제공하며, 메뉴 도구와 결과 ID를 내부 카탈로그로 제한한다. 저장소에서 하드코딩된 비밀은 확인되지 않았고 배포 토큰은 환경 변수 기반 비밀 참조로 설계됐다. 다만 Key Vault/관리 ID는 없고, 프롬프트 인젝션에 대한 명시적 탐지·차단, 입력 길이/허용값 검증, 호출 제한, 민감정보 삭제·보존 정책 및 보안 헤더 구성이 없다. 특히 자연어 채팅 UI는 API의 `ai_generated` 값을 표시하지 않으며, 퀴즈 모델 출력의 태그가 실제 허용 목록에 속하는지도 검증하지 않는다. 위험한 결제·주문 기능은 범위 밖이므로 별도 승인 단계가 필요할 행동은 확인되지 않았다.

Innovation & Originality
4/5
AI를 단순 채팅·요약에 붙인 복제품이 아니라, 자취생의 메뉴 결정 피로를 줄이는 적응형 퀴즈를 시작점으로 1인분 장보기 비용, 보유 재료 차감, 남은 재료의 다음 끼니 활용까지 하나의 의사결정 흐름으로 연결한 점이 창의적이어서 4점(독창적)에 해당합니다. 특히 AI가 다음 질문을 구조화 생성하고, 자연어 추천에서는 메뉴 검색과 비용 계산 도구를 조합하도록 역할을 나눈 설계가 제품 문제와 잘 맞습니다. 다만 추천 기반은 18개 정적 카탈로그와 비교적 단순한 태그 점수·재료 중복 규칙이며, 완전히 새로운 AI 기법이나 시장을 바꿀 수준의 메커니즘은 아니어서 5점까지는 어렵습니다.




---


종합 평가
PAGO는 낯선 기술 분야를 시장·학계·커뮤니티 관점에서 병렬 조사하고, 이를 근거 기반 학습 지도와 2주 브리프로 통합하는 문제 설정이 명확하고 독창적입니다. GitHub Copilot SDK를 유일한 모델 연결 계층으로 사용하고 Microsoft Agent Framework를 역할별 에이전트, 최소권한 도구 호출, 병렬 실행, 부분 실패 처리, 스트리밍 및 캐시에 실질적으로 활용한 점이 특히 강합니다. Azure Container Apps, ACR, Log Analytics와 Bicep/azd를 통한 실제 배포 구성도 확인되며, 근거 링크·단계별 탐색·실시간 상태 표시 등 생산성과 사용자 흐름을 고려한 설계가 돋보입니다.

그러나 공개 배포에서 핵심 진입 동작인 주제 클릭이 완료되지 않아 전체 사용자 여정과 주장한 시간 절감 효과를 입증하지 못했습니다. 재시도·취소 수단과 자동화 테스트가 부족하며, 전역 `FALLBACK_QUEUE`의 요청 간 혼선 가능성, 불완전한 출력 검증, 로컬 파일 캐시 및 단일 복제본 의존도 안정성을 낮춥니다. 또한 생성된 마크다운의 무정화 HTML 삽입에 따른 XSS 위험, 외부 데이터의 프롬프트 인젝션 방어 부족, 인증·속도 제한·CSP/HSTS·Key Vault·관리 ID 부재도 보완이 필요합니다.

가장 영향력 있는 개선은 공개 환경의 주제 클릭부터 브리프 생성까지 핵심 흐름을 우선 복구하고, 이를 실제 외부 의존성을 포함한 종단 간 자동화 테스트와 관측성으로 지속 검증하는 것입니다. 이후 요청별 이벤트 격리와 명시적 재시도 UX를 적용하고, HTML 정화·인증·속도 제한·관리 ID 및 Key Vault를 도입하면 현재의 강한 에이전트 설계와 참신한 경험이 신뢰할 수 있는 제품 가치로 이어질 것입니다.

Effective Use of Copilot SDK & Microsoft Agent Framework
3/5
GitHub Copilot SDK가 유일한 모델 연결 계층이고 Microsoft Agent Framework가 역할별 에이전트·도구 호출·스트리밍의 중심으로 구현되어 있어 코드상 통합 수준은 강합니다. 세 관점 에이전트의 병렬 fan-out, 통합 에이전트 fan-in, 근거 데이터 도구의 최소권한 배정, 부분 실패 처리, 브리프 델타 스트리밍, 캐시 및 컨텍스트 전달이 제품 가치에 직접 기여합니다. 다만 공개 앱에서 핵심 주제 클릭이 완료되지 않아 제출물이 종단 간 동작하지 않았다는 스카우트 판정이 `FAILED`이므로, 게이팅 규칙에 따라 3점으로 제한합니다.

Productivity Impact & Problem Fit
3/5
직무 전환 개발자, 기획·투자 담당자, 학생·연구 입문자라는 사용자가 명확하고, 낯선 분야에서 뉴스·논문·커뮤니티를 따로 조사하고 우선순위를 정하는 데 수 시간이 드는 문제를 ‘클릭 몇 번, 약 1분’으로 줄인다는 전후 효과도 구체적입니다. 실제 구현도 병렬 수집, 근거 링크, 단계별 드릴다운, 2주 학습 로드맵까지 문제에 잘 맞게 연결되어 있어 생산성 향상 가능성은 충분합니다. 다만 절감 수치는 사용자 측정이나 비교 실험 없이 문서 주장에 머물고, 공개 배포에서는 첫 주제 클릭부터 완료되지 않아 핵심 효익을 실제로 입증하지 못했습니다. 따라서 명확한 문제 적합성과 구현 근거는 있으나 효과가 부분적으로만 시연된 ‘그럴듯함’ 수준인 3점입니다.

Azure Cloud Integration
3/5
Azure Container Apps에 실제 배포되어 있고, ACR 및 Log Analytics를 Bicep/azd로 함께 구성해 ‘배포됨’ 수준에는 해당합니다. 다만 클라우드 네이티브 보안·운영 성숙도는 제한적입니다. ACR 관리자 계정과 암호를 사용하고 관리 ID·Key Vault가 없으며, 상태 확인 경로는 존재하지만 인프라에 시작/준비/활성 프로브가 연결되지 않았습니다. 또한 단일 고정 복제본과 로컬 파일 캐시는 확장성·복원력이 낮고, CI는 이미지 푸시까지만 수행합니다. 무엇보다 작업 게이트가 실패했으므로 이 기준의 상한 3을 적용했습니다.

Functionality & Technical Execution
2/5
공개 페이지와 조회 API는 실행되지만, 핵심 진입 동작인 주제 클릭이 배포 환경에서 완료되지 않아 종단 간 기능성 게이트를 통과하지 못했습니다. 따라서 상한 3을 적용하며, 실제 핵심 흐름이 시작조차 되지 않은 상태는 ‘작동하지만 거친 수준’보다 ‘취약함’에 해당해 2점입니다. 코드에는 FastAPI 수명주기, SSE, 병렬 에이전트 실행, 캐시, 입력 제한, 예외 로깅 및 부분 실패 처리 등 설계상 강점이 있습니다. 다만 모델 응답의 하위 주제를 최대 5개로 자르기만 하고 정확히 5개인지 검증하지 않으며, 요청별 근거 이벤트의 전역 `FALLBACK_QUEUE`는 단일 레플리카에서도 동시 요청 간 혼선 가능성이 있고, 파일 캐시 쓰기 실패를 조용히 무시합니다. 자동화 테스트도 제출 트리에서 확인되지 않아 현재 배포 상호작용 실패를 방지할 안전망이 부족합니다.

User Experience & Workflow Design
2/5
공개 홈은 로그인 없이 목적, AI의 역할, 6개 주제와 출처를 명확히 보여 주지만, 핵심 첫 클릭이 실제 배포에서 완료되지 않아 사용자 여정이 시작 단계에서 막혔습니다. 구현에는 실시간 에이전트 상태, 스트리밍 브리프, 오류 토스트, 빈 상태, 브레드크럼 등 매끄러운 흐름을 위한 요소가 있으나, 실패 후 명시적 재시도·취소 수단이 없고 접근성 보완도 부족합니다. 따라서 실제 사용 경험은 ‘불편함’ 수준인 2점입니다.

Responsible AI, Security & Trust
3/5
기본 수준의 책임 있는 AI·보안 통제는 구현되어 있다. AI 생성물 표시, 근거 URL, 수치 환각 억제 지침, 읽기 전용 도구 제한, 입력 길이 검증, Azure Container Apps 비밀 주입이 확인되며 하드코딩된 토큰은 발견되지 않았다. 다만 외부 데이터의 프롬프트 인젝션을 단순히 JSON 데이터로 취급할 뿐 명시적 격리·검사 방어가 없고, AI가 생성한 마크다운을 정화 없이 HTML로 삽입해 XSS 위험이 있다. 공개 AI 엔드포인트의 인증·속도 제한, CSP/HSTS 설정, Key Vault·관리 ID도 확인되지 않는다. 따라서 일부 완화책은 있으나 4점의 충분히 고려된 보안 수준에는 미치지 못한다.

Innovation & Originality
4/5
4 — 독창적이다. PAGO는 흔한 문서 채팅·요약기를 넘어, 낯선 기술 분야를 시장·학계·커뮤니티의 세 신호로 병렬 조사하고, 통합 AI가 재귀적인 학습 지도를 만든 뒤 근거 기반 2주 브리프로 연결한다. 특히 점수를 버블 크기와 3색 도넛 링으로 표현하며 최대 4단계까지 파고드는 UX와, 공개 API 실측치를 에이전트별 도구 권한에 결합한 구성이 생산성 도구로서 참신하다. 다만 에이전트별 분석과 최종 점수는 비교적 전형적인 병렬 실행 후 통합 패턴이고, 공개 배포에서 핵심 클릭 이후 워크플로가 작동하는 모습이 확인되지 않아 획기적 수준인 5점에는 미치지 못한다.



---


Effective Use of Copilot SDK & Microsoft Agent Framework
5/5
GitHub Copilot SDK가 유일한 LLM 연결 계층이고 Microsoft Agent Framework의 `GitHubCopilotAgent`가 적응형 퀴즈·도구 기반 채팅·AI 코치를 실제로 수행한다. 단순 채팅 래퍼를 넘어 구조화 출력 검증, 카탈로그 제한 도구 호출, 최근 6턴 문맥, SSE 스트리밍, 작업별 타임아웃·재시도·폴백, 출처 표시까지 제품 핵심 흐름에 통합되어 있어 ‘탁월함’ 구간에 해당한다. 라이브 앱도 종단 간 동작 판정을 통과했으므로 상한 제한은 적용하지 않았다.

Productivity Impact & Problem Fit
4/5
대상 사용자가 1인 가구 자취생으로 명확하고, 메뉴 결정 피로와 1인분 장보기 비용 불확실성이라는 실제 문제를 하나의 짧은 흐름으로 연결한다. 기존 10분 이상에서 약 1분·90초 이내로 줄인다는 전후 기준이 있으며, 추천뿐 아니라 재료별 구매가, 1인분 환산가, 보유 재료 반영 재계산, 레시피와 다음 끼니까지 구현되어 실질적인 탐색·정리 작업 감소가 설득력 있다. 다만 10분이라는 기준의 출처나 사용자 검증 결과가 없고, 정찰 결과도 첫 질문까지만 확인했으며, 실제 김치찌개 예시의 판매단위 구매 총액은 31,000원으로 명시된 5천~1만원 사용자 예산과 간극이 있어 ‘명백하고 큰 효과’의 5점보다는 4점이 적절하다.

Azure Cloud Integration
5/5
Azure Container Apps, ACR, Key Vault, 관리 ID/RBAC, Log Analytics 및 Application Insights가 하나의 Bicep 청사진으로 응집되어 있고, 실제 배포에서도 상태 점검과 Azure Monitor 연결이 확인됩니다. 최소 권한 이미지·비밀 접근, 상태 프로브, 자동 확장, 저비용 리소스 크기, 불변 이미지 기반 OIDC 배포까지 갖춰 ‘Cloud-Native’ 수준입니다. 다만 CI는 기존 인프라에 앱 이미지를 갱신할 뿐 Bicep 자체를 적용하지 않고, TRD의 Application Insights 연결 문자열 Key Vault 참조 주장과 달리 현재 Bicep은 이를 환경 변수 값으로 직접 설정한다는 작은 불일치가 있습니다. 앱은 작업 게이트를 통과했으므로 점수 상한은 적용하지 않았습니다.

Functionality & Technical Execution
4/5
배포 앱이 정상 실행되고 무로그인 핵심 퀴즈 진입이 확인되었으며, 구현은 프론트엔드·API·에이전트·안전 검증·관측성·배포 인프라로 명확히 분리되어 있습니다. 입력 검증, 표준 오류 응답, 요청 ID/보안 헤더, 속도 제한, AI 타임아웃·재시도·규칙 기반 폴백, 사용자 취소, 상태 경합 방지까지 갖춰 ‘Robust’ 수준입니다. 다만 자동화 테스트가 전체 퀴즈→추천→장보기 재계산 흐름과 AI 실패/타임아웃을 실제로 관통하지 않고, 클라이언트의 로컬 폴백은 첫 질문 이후 AI 요청을 취소하거나 실패하면 바로 추천으로 넘어갈 수 있어 PRD의 4~6문항 보장이 약합니다. 따라서 안정적이지만 완전히 다듬어진 5점보다는 4점이 적절합니다.

User Experience & Workflow Design
4/5
핵심 흐름이 무로그인 단일 화면에서 자연스럽게 시작되고, 퀴즈·자연어 채팅·추천 결과·장보기 비용 재계산까지 사용자 선택지가 명확합니다. AI 생성/규칙 기반 출처를 시각적으로 구분하며, 대기 문구·스트리밍·타임아웃·취소·재시도·폴백을 갖춰 지연과 오류 대응도 매우 충실합니다. 다만 장보기 체크박스의 시각적 의미와 접근성 이름이 서로 반대이고, 채팅 상세 이동 요소의 키보드 접근 및 대화상자 초점 관리가 빠져 있어 ‘매끄러움’에는 해당하지만 ‘즐거울 정도로 완성된’ 5점 수준에는 미치지 못합니다.

Responsible AI, Security & Trust
5/5
최상 수준입니다. AI 생성 여부와 폴백 출처, 가격의 예상치 한계를 UI와 API에서 명확히 구분하고, 사용자에게 AI 대기 취소권을 제공합니다. 프롬프트 인젝션 사전 차단, 허용 목록 기반 도구·출력 검증, 입력 길이·역할 제한, 속도 제한을 함께 적용했습니다. 무로그인·무결제 구조와 원문 미수집 텔레메트리로 개인정보 및 고위험 행위를 최소화하며, 비밀은 Key Vault와 관리 ID/OIDC로 처리합니다. 제출 트리에서 하드코딩된 토큰·키는 발견되지 않았습니다.

Innovation & Originality
4/5
‘창의적’ 구간의 4점입니다. 한끼픽은 1인 가구의 메뉴 결정과 실제 구매 단위 비용·1인분 환산 비용·남은 재료 활용을 하나의 짧은 여정으로 묶고, 고정 설문이 아니라 답변 문맥에 따라 다음 질문 자체를 생성하는 AI를 핵심 상호작용으로 삼았습니다. 흔한 문서 채팅·요약·티켓 분류형 보조도구와 달리, 적응형 질문 생성 → 규칙 기반 카탈로그 추천 → 장보기 비용 계산 → 다음 끼니 제안으로 이어지는 도구 조합과 ‘AI 대기를 취소하고 즉시 규칙 흐름으로 진행’하는 UX가 독창적이며 실제 코드가 이를 뒷받침합니다. 다만 메뉴 후보가 18종의 정적 카탈로그이고 최종 추천·비용·다음 끼니 계산은 주로 규칙 기반이어서, 새로운 AI 패러다임을 제시하는 5점 수준까지는 아닙니다.