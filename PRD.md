# mat-copilot — 통합 PRD (Product Requirements Document)

| 항목 | 내용 |
| --- | --- |
| 문서 버전 | v1.0 (통합본) |
| 작성자 | @sw1029 |
| 최종 수정일 | 2026-08-22 |
| 통합 원본 | [PRD/back.md](PRD/back.md) v0.4 (Backend PRD), [PRD/front.md](PRD/front.md) v0.2 (Frontend Design PRD) |
| 관련 문서 | [TRD.md](TRD.md), [SCHEMA/schema.md](SCHEMA/schema.md), [GAP_ANALYSIS.md](GAP_ANALYSIS.md), [AGENTS.md](AGENTS.md), [regulations.md](regulations.md) |

> 본 문서는 `PRD/` 디렉토리의 백엔드 PRD(v0.4)와 프론트엔드 PRD(v0.2)를 **누락 없이 통합**한 프로젝트 루트 단일 PRD다.
> **§I 통합 개요**가 두 문서를 관통하는 제품 정의·흐름·범위·마일스톤을 종합하고, **제1부(백엔드)·제2부(프론트엔드)**에 각 원본의 전문을 수록한다. 수록 시 상대 링크만 루트 기준 경로로 치환했으며 그 외 내용은 원문 그대로다. 원본 문서는 `PRD/` 디렉토리에 그대로 유지된다.
> FR/US/NG/AC 등 요구사항 ID는 각 부(원본 문서) 내부에서 유효하다 — 교차 참조 시 "백 FR-n"/"프론트 FR-n"으로 구분한다.

---

## I. 통합 개요

### I.1 제품 정의

**mat-copilot**은 AI agent가 ① 기획안 업로드(docx/txt/md) 또는 ② deep interview(다중 agent의 반복 질의·검증 트리)로 사용자의 모호한 의도를 구체화하여 **의도 기준선 문서(IntentDoc, Markdown)** 로 정리하고, ③ 사용자가 제출한 결과물(파일·웹 링크·GitHub)이 그 의도 대비 **얼마나·어느 지점에서 어긋났는지(drift)** 를 근거 인용과 함께 정량(개수 기반)/정성 분석·보고하며 개선을 제안하는 **로그인 없는 웹앱**이다.

| 문제 (배경) | 해결 방향 |
| --- | --- |
| 사용자(특히 프로젝트 진행 초심자)의 초기 의도는 모호하며, 초기 설계가 미흡하거나 AI에게 문서·코드·기타 작업을 위임하는 과정에서 요구 누락, 의미 변형(의도 왜곡), 범위 초과, 할루시네이션이 발생한다. | 기획안 추출 또는 deep interview로 의도 기준선을 확보하고, 반복 질의에서 유저의 의식적/무의식적 의도까지 정성 추출한다. |
| 결과물이 원래 의도와 어디서 어긋났는지 수동 대조하면 문서 10페이지·코드 수백 라인 기준 30~60분 이상 걸리고 근거 누락 위험이 크다. | 결과물 제출 후 **수 분 내** 근거 위치가 인용된 drift 보고서(테마별 finding + 개수 기반 지표 + 개선 제안)를 반환한다. |
| 단순 점수·요약만으로는 어떤 결과물 위치가 어떤 의도와 충돌하는지 판단하기 어렵고, 긴 대기 중 서비스가 멈춘 것으로 오해하기 쉽다. | 모든 판정에 결과물 내 근거 위치 인용을 의무화(근거 없으면 저신뢰 표기)하고, IntentDoc 블록↔근거를 각주로 연결하며, 분석 단계를 투명하게 표시한다. |

### I.2 통합 사용자 흐름 (End-to-End)

| 단계 | 사용자 행동 | 백엔드 (제1부) | 프론트엔드 (제2부) |
| --- | --- | --- | --- |
| 1. 홈 진입 | 시작 경로 선택 | 세션 token 발급 (백 FR-11) | `기획안 업로드`·`질문으로 시작`·`샘플로 체험` CTA, 세션 복구 배너 (프론트 FR-1~2) |
| 2. 의도 확보 | 기획안 업로드 또는 질문에 순차 답변 | agent 기획 추출(백 FR-1) / 다중 agent 질문 생성·검증, confused 지표·종료조건 제어(백 FR-2~4) | 업로드 사전 검증(프론트 FR-3), 한 번에 하나의 활성 질문 + 마인드맵 렌더링, 조기 종료(프론트 FR-4~10) |
| 3. 결과물 제출 | 파일·HTTPS 링크·GitHub 다건 제출 | sandbox 정적 파싱, read-only 수집, ingestStatus 기록 (백 FR-7) | 한도 사전 검증·수집 상태 목록·분석 시작 (프론트 FR-11) |
| 4. 분석 대기 | 진행 확인, 필요 시 취소 | 비동기 job 6단계 파이프라인: 정규화(FR-5)→평가(FR-6)→테마별 drift fan-out(FR-8)→집계(FR-9)→보고서(FR-10) | JobStage를 3단계 라벨로 표시, 2초 폴링+ETag, 취소·재시도 (프론트 FR-12~13) |
| 5. 보고서 | 결과 판독·다운로드·삭제 | IntentDoc·finding·evidence·metrics·suggestions·정규화 schema 동봉 반환 | 30:70 분할 화면, 각주·교차 강조, 차트, 산정 불가 표시, md 다운로드, AI 고지 (프론트 FR-15~24) |

**세션 상태 정합** (백 `SessionStatus` ↔ 프론트 상태 ↔ 대표 화면):

| 백 SessionStatus | 프론트 상태 | 대표 화면 |
| --- | --- | --- |
| CREATED | INITIAL | 홈 (의도 확보 전) |
| INTERVIEWING | INTERVIEWING | 연속 질의 마인드맵 |
| INTERVIEW_DONE | SUBMITTING | 결과물 제출 |
| ANALYZING | ANALYZING | 분석 대기 |
| REPORT_READY | COMPLETED | 분석 결과 |
| EXPIRED / 404 | EXPIRED | 만료 안내 (새 세션 CTA) |

### I.3 백엔드/프론트엔드 역할 분담

| 구분 | 백엔드 (제1부 범위) | 프론트엔드 (제2부 범위) |
| --- | --- | --- |
| 담당 | 세션/token 관리, agent 오케스트레이션(질문 생성·검증, 정규화, 평가, 테마별 drift, 보고서), sandbox 파싱, 개수 기반 정량 집계, 도표 구성 데이터(ChartSpec: 축 이름·csv) 제공 | React 기반 전체 화면 고정형 SPA: 마인드맵·제출·대기·결과 화면, IntentDoc/근거 각주·교차 강조, 차트 렌더링(chart type 자율), Markdown 다운로드, AI 고지, 세션 복구 |
| 하지 않는 것 | 시각화 렌더링(백 NG3), 결과물 자동 수정(백 NG4), 인증/인가(백 NG1), heavy job 실행(백 NG2), 세션 간 학습/비교(백 NG5), 운영/관리자 기능(백 NG6) | LLM 추론·drift 판정·파일 파싱·정량 산식(프론트 NG1), 계정/이력 관리(프론트 NG2), 노드 임의 편집(프론트 NG3), 결과물 자동 수정(프론트 NG4), 모바일 전용 앱(프론트 NG5) |

### I.4 공통 제약 (AGENTS.md)

- **Copilot SDK 사용 필수** — 백엔드 전 agent의 모델 연결 계층.
- **Microsoft Agent Framework 사용 필수** — agent 정의·오케스트레이션. 프론트는 해당 결과를 표시하는 클라이언트(프론트 CON-2).
- **웹앱 + Azure 클라우드 배포** — 프론트 정적 번들은 백엔드 컨테이너(Azure Container Apps)에서 단일 URL로 서빙.
- **로그인 없이 동작** — 발급형 세션 토큰만으로 식별. TTL: 마지막 활동 기준 24h, 보고서 도달 시 72h. `내 데이터 지우기`로 즉시 파기.
- **해커톤 MVP** — [matdathon-2026 심사 기준](https://github.com/matdaaiga-kr/matdathon-2026/blob/main/judgement/judgement-criteria.md) 대응, MVP 수준 구현.

### I.5 요구사항 상호 매핑 (개요)

| 백엔드 FR | 대응 프론트엔드 FR/AC | 통합 의미 |
| --- | --- | --- |
| 백 FR-1 기획안 업로드/추출 | 프론트 FR-2, FR-3 | 홈 업로드 경로 + `.docx/.txt/.md` 10MB 사전 검증 |
| 백 FR-2~4 deep interview·종료조건·의도 변경 감지 | 프론트 FR-4~10 | 첫 질문, 순차 답변, 마인드맵 트리 렌더, 활성 질문 1개, 조기 종료(REQUIRED 경고) |
| 백 FR-5, FR-6 정규화·정량/정성 평가 | (서버 내부) 프론트 FR-16, FR-20 | 정규화 schema는 보고서 동봉으로 노출, IntentDoc·finding 렌더 |
| 백 FR-7 결과물 수집(sandbox/웹/GitHub) | 프론트 FR-11 | 다건 제출, ingestStatus별 사용자 문구 |
| 백 FR-8 drift 분석(코어 테마+근거 인용 의무) | 프론트 FR-17, FR-18, FR-20 | 각주 파생, 교차 강조, 심각도/신뢰도/근거 인용 표시 |
| 백 FR-9 집계/시각화 데이터 | 프론트 FR-19 | 개수·비율 metrics + 차트 1종 이상 (임계값·상태는 백 메타 사용) |
| 백 FR-10 보고서 | 프론트 FR-15, FR-16, FR-20, FR-23~24 | 30:70 레이아웃, 상세 리포트, md 다운로드, 빈 상태 안내 |
| 백 FR-11 세션 관리 | 프론트 FR-1, FR-14, FR-22, FR-25 | 토큰 보관·복구 라우팅·오류/재시도·이탈 방지·데이터 삭제 |
| 백 FR-12 데모 샘플 경로 | 프론트 FR-2(샘플 체험), AC-2 | 심사자 3분 내 완주 데모 + LLM 장애 시연 안전장치 |
| 백 §7 보안/책임 있는 AI | 프론트 FR-21 | 모든 AI 생성 표면에 고지 배지/문구 |

### I.6 합동 마일스톤

> 백 PRD v0.4에서 프론트와 **합동 MVP**로 재정의 — 제품 코어(deep interview·다중 agent drift fan-out)가 M1에서 시연 가능해야 한다(GAP_ANALYSIS X-3 해소).

| 단계 | 통합 범위 | 완료 조건 |
| --- | --- | --- |
| **M1 — 합동 MVP** | 세션 관리, 기획안 업로드/추출, **deep interview 최소형**(깊이≤2·질문≤15·조기 종료), 정보 정규화, 정량/정성 평가, 파일 결과물 sandbox 분석, **drift 코어 2종**(요구 누락·의도 왜곡), IntentDoc, 보고서(metrics·차트 1종), 데모 샘플 경로, Azure 배포+상태 확인 | 로그인 없이 샘플 또는 사용자 입력으로 홈부터 결과 화면까지 완주 |
| **M2 — 분석 확장** | 인터뷰 확장(깊이 3, SSE 검토, 답변 수정), 웹 페이지·GitHub 수집, 코어 4종+동적 보조 테마, 시각화·차트 확장, pdf 포맷 검토, 골든셋 기반 정확도 지표 | 다양한 결과물 유형과 복잡한 질문 흐름의 안정 처리 |
| **M3 — 품질 개선** (프론트) | 접근성 세부 개선, 성능 최적화, 관측성 대시보드, 빈 상태·세부 UX 개선 | 지원 브라우저·해상도·모바일 핵심 판독 경로 품질 기준 충족 |

### I.7 통합 성공 지표 하이라이트

- **가치 가설(전후 효과)**: 수작업 대조(체감 수십 분)를 → 근거 인용 drift 보고서 **수 분 내** 반환으로 단축. 목표 수치는 베이스라인 측정 후 설정.
- **심사 데모**: 샘플 체험 완주 3분 이내 (백 US-7 ↔ 프론트 성공 지표).
- **신뢰**: 근거 인용 검증 통과율 측정, 분석 완료율 95% 이상(프론트), 새로고침 복구 성공률 99% 이상(프론트).
- 상세 지표 표는 제1부 §8, 제2부 §10 참조.

### I.8 문서 구성

| 부 | 원본 | 내용 |
| --- | --- | --- |
| 제1부 | `PRD/back.md` v0.4 | 백엔드 제품 요구사항 전문 — 개요, 목표/비목표, 사용자 스토리, FR-1~12, API/데이터 모델 개요, NFR, 성공 지표, 제약/가정, 리스크, 마일스톤, 미결 사항 |
| 제2부 | `PRD/front.md` v0.2 | 프론트엔드 디자인 PRD 전문 — 정보 구조/흐름, 화면별 디자인 요구사항, US/FR-1~25, 데이터 계약·검증 상수·오류 상태·복구 라우팅, NFR, 성공 지표, 제약/가정, 리스크, 마일스톤, 수용 기준 AC-1~23, 미결 사항 |

---


---

> ## 제1부 — Backend PRD
> 원본: [`PRD/back.md`](PRD/back.md) v0.4 — 이하 원문 전문 수록 (상대 링크만 루트 기준으로 치환, 내용 무변경)

# Backend PRD — mat-copilot

| 항목 | 내용 |
| --- | --- |
| 문서 버전 | v0.4 (Draft) — GAP_ANALYSIS 반영: 합동 MVP 재정의(M1 인터뷰 최소형 포함), 데모 경로 신설, 보존·방어 정책 확정, 미결 상태 갱신 |
| 작성자 | @sw1029 |
| 최종 수정일 | 2026-08-22 |
| 관련 문서 | [PRD/front.md](PRD/front.md), [TRD/back.md](TRD/back.md), [SCHEMA/schema.md](SCHEMA/schema.md), [GAP_ANALYSIS.md](GAP_ANALYSIS.md) |

## 1. 개요 (Overview)
초기 유저 의도 정보가 주어진 경우 agent 기반 하위 질문 n개 생성 -> 각 질문별 하위질문 branch를 모호한 점이 해결될 때까지 지속 생성하여 모호한 유저 의도를 구체화한 뒤, 제공된 링크/파일의 결과물을 토대로 해당 초기 의도가 충족되었는지, 얼마나, 어느 지점에서 어긋났는지 확인 후 정량적 통계 생성 및 정성/정량 보고서 반환

## 2. 배경 및 문제 정의 (Background & Problem)
- 현재 상황: 유저 초기 의도는 모호하며, 초기 설계가 미흡하거나, 초기 설계를 원활히 수행하였더라도 ai에게 위임하는 경우 문서작업, 코딩, 기타 작업 등에서 발생하는 초기 의도와 다른 실수(의도되지 않은) 및 ai에게 작업을 위임한 경우 발생하는 할루시네이션 등을 사용자가 한 눈에 파악하기 힘들다. 
- 해결하려는 문제: 유저 초기 의도를 반복된 질의(deep interview)를 통해 상세 구체화 및 반복되는 질의에서 유형/무형적으로 확인 가능한 의식적/무의식적 유저의 의도를 추출하여 해당 의도 대비 유저가 제공한 결과물(문서, 작업 결과물, 코딩 결과물 등)가 얼마나 어긋난 형태인지 상세 보고하고, 해당 결과를 바탕으로 개선제안

## 3. 목표 및 비목표 (Goals & Non-Goals)
### 3.1 목표
- G1. 유저가 올린 docx/txt/md 등의 포맷을 가지는 기획안 등에서 tool use가 가능한 agent를 통해 초기 기획 추출
- G2. 초기 기획안이 없는 경우 agent를 통해 반복적 질의(이 경우 질문 담당 agent는 하나에 그치지 않고 여러 agent를 통해 질문 생성/검증 수행). 중간에 초기 의도 대비 변경된 요소가 있는지도 추가 질문(이를 통해 user의 confused 지점 파악 + 해당 지점 바탕으로 유저의 의식적/무의식적 의도 방향성 정성 추출)
- G3. 추출한 정보를 정규화
- G4. 정규화된 정보를 바탕으로 원하는 정량/정성 정보 추출
- G5. 추출된 정보를 태그/스키마/agent를 통한 정량/정성평가 정보를 도출
- G6. 도출된 정보를 토대로 유저가 제공한 결과물(문서, 작업 결과물, 코딩 결과물 등)가 얼마나 어긋난 형태인지 agent(분석 테마별 agent를 별도 운용)를 통해 분석
- G7. 분석결과를 바탕으로 정량 집계/정성 분석 결과를 시각화(이 경우 분석 테마별 agent를 따로 둘것)
- G8. 멀티테넌시/동시성 지원 — 로그인 없이 세션 단위로 식별되는 다중 유저의 동시 세션 처리

### 3.2 비목표
- NG1. 인증/인가는 수행하지 않음 (MVP 구현 우선, AGENTS.md "로그인 없이 동작" 제약과 정합)
- NG2. heavy job은 직접 실행하지 않음
- NG3. 시각화 렌더링은 백엔드 범위가 아님 (백엔드는 도표 구성용 데이터만 제공, 렌더링은 프론트 담당)
- NG4. 결과물의 자동 수정/수정 실행은 하지 않음 (개선 "제안"까지가 범위)
- NG5. 세션 간 유저 프로파일 학습/비교는 하지 않음 (schema/tag의 세션 단위 임의 생성과 정합)
- NG6. 운영/관리자 기능은 제공하지 않음 (해커톤 구현)

## 4. 사용자 및 이해관계자 (Users & Stakeholders)
| 구분 | 설명 | 백엔드에 대한 기대 |
| --- | --- | --- |
| 최종 사용자 | 구조화되지 않은 형식으로 즉흥적으로 작업을 수행하는 사용자 (ai에게 작업을 위임하는 경우 포함) | 초기 의도, 중간 의도 대비 최종 결과물이 얼마나 drift되었는지에 대한 정량/정성 분석 및 개선제안 제공 |
| 프론트엔드 | 인터뷰(질의응답) 진행 및 보고서를 가변 렌더링하는 클라이언트 | 질의/응답 루프, 유저 설정(질문 강도·시간 제한·request flag), 기획안/결과물 제출, 보고서 조회를 위한 API 및 plot용 도표 구성 정보(x/y축 이름, csv로 구성 가능한 정보) 제공 |
| 운영/관리자 | 해당 없음 — 해커톤 구현으로 제외 (NG6) | 없음 |

## 5. 핵심 시나리오 (User Stories)
| ID | 사용자 스토리 | 우선순위 |
| --- | --- | --- |
| US-1 | 기획안(docx/txt/md)을 보유한 사용자로서, 업로드한 기획안에서 agent가 초기 기획을 추출해 주기를 원한다. 왜냐하면 초기 의도를 별도 재작성 없이 분석 기준선으로 삼고 싶기 때문이다. (G1) | P0 |
| US-2 | 초기 기획안이 없는 사용자로서, 여러 agent의 반복적 질의(deep interview)를 통해 모호한 의도를 구체화하고 싶다. (G2) | P1 |
| US-3 | 작업 중 의도가 변한 사용자로서, 초기 의도 대비 변경된 요소에 대한 추가 질문을 받아 confused 지점과 의식적/무의식적 의도 방향성이 분석에 반영되기를 원한다. (G2) | P1 |
| US-4 | 사용자로서, 질문 강도(confuse threshold)와 시간 제한을 설정하고, 더 대답하고 싶은 요소가 있으면 request flag로 추가 구체화를 진행하고 싶다. | P1 |
| US-5 | 결과물(링크/파일)을 제출하는 사용자로서, 초기/중간 의도 대비 결과물이 얼마나·어느 지점에서 어긋났는지 정량/정성 분석을 받고 싶다. (G6) | P0 |
| US-6 | 사용자로서, 정량 통계와 정성/정량 보고서 및 개선제안을 받고 싶다. (G7) | P0 |
| US-7 | 처음 방문한 사용자(심사자 포함)로서, 내 자료 없이도 사전 준비된 샘플 데이터로 전체 흐름(의도 기준선 → 결과물 분석 → 보고서)을 수 분 내에 체험하고 싶다. | P1 |

## 6. 기능 요구사항 (Functional Requirements)
| ID | 기능 | 설명 | 우선순위 |
| --- | --- | --- | --- |
| FR-1 | 기획안 업로드/추출 | docx/txt/md 포맷의 기획안을 수신하고 tool use가 가능한 agent로 초기 기획 추출 (MVP는 3종 고정, pdf 등 추가 포맷은 M2 검토) | P0 |
| FR-2 | Deep interview | 초기 기획안이 없는 경우 여러 agent를 통한 질문 생성/검증, 각 질문별 하위질문 branch를 지속 생성하여 의도 구체화 | P1 |
| FR-3 | 인터뷰 종료조건 제어 | 각 가지 질문의 confused 지표(agent가 산출)를 통해 하위 질문 생성 필요가 더 이상 제기되지 않을 때까지 + 유저가 설정한 질문 강도(confuse의 threshold 조정) 기준으로 종료. 단, 결과 산출에 필수적인 정성 결과는 agent 판단 하에 질문 가능하며, 필수 질문 외의 추가 임의 질문만 해당 종료조건의 대상. 감시 agent 혹은 임의 종료조건을 할당해 지나치게 길어지는 것을 방지하되, 유저 request flag가 있으면 추가 구체화 가능. confused 지표는 질문 생성 agent와 분리된 검증 agent가 가지(노드) 단위의 연속값(0~1)으로 산출하고 질문 강도는 그 threshold로 동작 (산출 산식 상세는 TRD) | P1 |
| FR-4 | 중간 의도 변경 감지 | 초기 의도 대비 변경된 요소에 대한 추가 질문으로 confused 지점 파악 + 해당 지점 바탕으로 유저의 의식적/무의식적 의도 방향성 정성 추출 | P1 |
| FR-5 | 정보 정규화 | 추출한 정보를 정규화. 정규화 schema/tag는 임의 생성하되 세션 내에서는 잠금(생성 후 고정)하며, 사용된 schema 명세를 보고서에 동봉 | P0 |
| FR-6 | 정량/정성 평가 | 정규화된 정보를 바탕으로 태그/스키마/agent를 통해 정량/정성 평가 정보 도출 | P0 |
| FR-7 | 결과물 수집/분석 실행 | 웹 페이지는 agent를 통해 분석. zip, docx 및 문서, 코드 파일 등(텍스트로 읽히는 파일 전반)은 sandbox로 분석. github는 read only로 분석. heavy job은 직접 실행하지 않음 | P0 (파일) / P1 (웹 페이지·github) |
| FR-8 | Drift 분석 | 분석 테마별 별도 agent를 운용하여 결과물이 의도 대비 얼마나·어느 지점에서 어긋났는지 분석. 테마는 고정 코어 4종(요구 누락, 의도 왜곡, 할루시네이션, 범위 초과) + agent가 동적 생성하는 보조 테마. 모든 판정에 결과물 내 근거 위치 인용을 의무화하고 근거가 없으면 저신뢰로 표기 | P0 (코어 2종: 요구 누락·의도 왜곡) / P1 (코어 전체+동적 보조) |
| FR-9 | 집계/시각화 데이터 생성 | 정량 집계 결과와 plot을 위한 도표 구성 정보(x/y축 이름) 및 csv로 구성될 수 있는 정보 생성. chart type 선택은 프론트 자율(가변 렌더링) | P1 |
| FR-10 | 보고서 생성 | 정량 통계 및 정성/정량 보고서와 개선제안 반환. 정량 지표는 개수 기반(어긋난 지점 수, 커버된 요구 수 등)으로 한정하고 점수화는 정성 참고로만 사용 | P0 |
| FR-11 | 세션 관리 | 로그인 없이 session token 발급/식별 (AGENTS.md "로그인 없이 동작" 충족 수단), 다중 유저 동시 세션(멀티테넌시) 지원 (G8). 세션 데이터는 한시 보관 후 자동 파기하며 유저가 즉시 삭제할 수 있음 (§7 보안) | P0 |
| FR-12 | 데모 샘플 경로 | 사전 준비된 샘플 기획안·결과물·보고서를 원클릭으로 로드하는 경로 제공 — 심사·첫 방문 시 수 분 내 전체 가치 체험(US-7). LLM 장애 시에도 동작하는 시연 안전장치를 겸함 | P1 |

### 6.1 API 개요
구체적인 Method/Endpoint 및 통신규약은 [SCHEMA/schema.md](SCHEMA/schema.md), [TRD/back.md](TRD/back.md)에서 확정. 인터뷰 질의/응답은 동기 또는 SSE 스트리밍, 분석 실행은 비동기 job + 상태 조회의 이원 구조. 인증/인가는 수행하지 않고 발급형 session token으로 세션을 식별.

| 기능 영역 | 설명 | 인증 |
| --- | --- | --- |
| 세션 발급·삭제 | 로그인 없이 session token 발급, 유저 주도 세션 데이터 즉시 삭제 (FR-11) | 불필요 |
| 기획안 업로드 | docx/txt/md 기획안 제출 (FR-1) | 불필요 |
| 인터뷰 질의/응답 | 질문 수신 및 답변 제출 루프 — 동기 또는 SSE, 유저 주도 조기 종료 포함 (FR-2~4) | 불필요 |
| 유저 설정 | 질문 강도(confuse threshold), 시간 제한, request flag (FR-3) | 불필요 |
| 결과물 제출 | 링크(웹 페이지/github)·파일(zip, docx, 문서, 코드) 제출, 비동기 분석 job 생성·취소 (FR-7) | 불필요 |
| 분석 결과/보고서 조회 | 분석 job 상태 조회, 정량 집계, 도표 구성 정보(x/y축 이름, csv 구성 가능 정보), 정성/정량 보고서, 개선제안 (FR-8~10) | 불필요 |
| 운영 상태 확인 | 서비스 헬스/레디니스 노출 — 심사·운영 중 장애 판별 | 불필요 |

### 6.2 데이터 모델 개요
<!-- 주요 엔티티와 관계를 간단히 기술 (상세 스키마는 TRD) -->
- 인터뷰 세션: session token으로 식별. 유저 설정(질문 강도, 시간 제한) 포함. 질문 트리와 결과물, 보고서의 상위 단위
- 의도: 초기 의도(기획안 추출 또는 인터뷰 도출) 및 중간 변경 의도, 의식적/무의식적 의도 방향성(정성)
- 질문/답변: 질문별 하위질문 branch(트리 구조), 각 가지의 confused 지표, 필수/임의 질문 구분, request flag
- 정규화 정보: 임의 생성된 schema/tag로 정규화된 의도 정보
- 결과물: 웹 페이지 링크, github(read only), 파일(zip/docx/문서/코드)
- 분석 결과: 테마별(코어 4종 + 동적 보조) agent의 정량/정성 결과와 근거 인용, 비동기 분석 job 상태
- 보고서: 정량 통계, 정성 분석, 개선제안, 도표 구성 정보(x/y축 이름, csv 구성 가능 정보)

## 7. 비기능 요구사항 (Non-Functional Requirements)
| 항목 | 기준 |
| --- | --- |
| 성능 | 인터뷰 턴은 대화형 응답(동기/SSE), 분석은 비동기 job으로 처리. 시간 제한은 유저 설정에 따라 가변이며 고정 수치 목표는 두지 않음 (해커톤 구현) |
| 가용성 | 수치 SLA 미설정. Azure 클라우드에 배포된 웹앱으로 동작해야 함 (AGENTS.md) |
| 확장성 | 멀티테넌시/동시 세션 지원 (G8). 구체 동시 세션 규모 목표는 미정 (§12) |
| 보안 | 인증/인가 미수행, 로그인 없이 동작 (AGENTS.md). 파일/코드는 sandbox 내 분석, github는 read only, heavy job 직접 실행 금지. 세션 간 학습/비교 없음(NG5). 세션 데이터는 한시 보관(마지막 활동 기준 24h, 보고서 완료 후 72h) 후 자동 파기하고 유저 즉시 삭제를 제공. 무인증 공개 API의 남용 방지(세션 발급 rate limit, 입력 상한) 적용 — 세부는 TRD §10 |
| 관측성 | agent 호출 trace(세션별), LLM 토큰/비용 로그, sandbox 실행 로그를 MVP부터 수집 |
| 책임 있는 AI | AI 생성 산출물(인터뷰 질문·보고서)에 AI 생성 고지 표기, 판정 근거 인용 의무, 자동 수정 없음(NG4) — 세부는 TRD §10.6 |

## 8. 성공 지표 (Success Metrics)

**가치 가설 (전후 효과 주장)**: 의도-결과물 대조를 수작업으로 수행하면 문서 전체를 교차 대독해야 하나(체감 수십 분 단위), 본 서비스는 인터뷰로 확정한 의도 기준선 대비 **근거 위치가 인용된 drift 보고서를 수 분 내** 반환한다 — "얼마나 어긋났는지"를 찾는 시간을 대조 작업에서 검토 작업 수준으로 단축한다. 목표 수치는 베이스라인 측정 후 설정(§12).

| 지표 | 현재 | 목표 |
| --- | --- | --- |
| 세션 완료율 (중도 이탈 없이 보고서 도달) | - (신규) | 베이스라인 측정 후 설정 |
| 제출~보고서 도달 시간 (전후 효과 주장의 검증 지표) | - (신규) | 베이스라인 측정 후 설정 (수 분 내 지향) |
| 근거 인용 검증 통과율 (finding 중 근거가 원문 대조로 확인된 비율) | - (신규) | 베이스라인 측정 후 설정 |
| 샘플 체험(US-7) 완주 시간 | - (신규) | 3분 내 지향 (심사 데모 경로) |
| 보고서 유용성 유저 평가 | - (신규) | 베이스라인 측정 후 설정 |
| 골든셋 대비 drift 검출 정확도/재현율 | - | M2에서 정의 |

## 9. 제약 및 가정 (Constraints & Assumptions)
- 제약:
  - copilot sdk 사용 필수 (AGENTS.md)
  - Microsoft Agent Framework 사용 필수 (AGENTS.md)
  - 최종 산출물은 웹앱이어야 하며 Azure 클라우드에 배포, 로그인 없이 동작 (AGENTS.md)
  - 해커톤 구현 — 기간/리소스 제한, 운영 조직 부재
  - 인증/인가 미구현 (MVP 구현 우선)
  - heavy job은 직접 실행하지 않음
  - github 결과물은 read only로만 접근
  - zip/docx/문서/코드 파일 분석은 sandbox 내에서 수행
  - 백엔드는 시각화 렌더링을 하지 않으며 도표 구성용 데이터(x/y축 이름, csv 구성 가능 정보)만 제공
- 가정:
  - 유저는 결과물을 링크(웹 페이지/github) 또는 파일 형태로 제공 가능
  - 정규화 schema/tag는 사전 고정이 아닌 임의 생성으로 충분함
  - LLM 기반 agent(tool use, 다중 agent 운용) 사용을 전제

## 10. 의존성 및 리스크 (Dependencies & Risks)
| 항목 | 내용 | 대응 방안 |
| --- | --- | --- |
| 의존성 | copilot sdk, Microsoft Agent Framework, Azure 클라우드 인프라, 문서 파서(docx 등), sandbox 실행 환경, github read only 접근 | 필수 스택은 AGENTS.md로 확정, 세부 구성은 TRD에서 정의 |
| 의존성 | 관련 문서 확정 상태 | 통신규약은 [SCHEMA/schema.md](SCHEMA/schema.md) v0.3, 기술 상세는 [TRD/back.md](TRD/back.md) v0.3에서 확정됨. 프론트 정합은 [PRD/front.md](PRD/front.md) v0.2·[TRD/front.md](TRD/front.md) v0.1과 상호 참조 |
| 리스크 | 인터뷰가 지나치게 길어질 수 있음 | 감시 agent 혹은 임의 종료조건 할당 + 유저 설정 질문 강도(confuse threshold)로 제어 |
| 리스크 | 신뢰할 수 없는 유저 제공 파일/코드의 실행 위험 | sandbox 분석, github read only, heavy job 직접 실행 금지 |
| 리스크 | 평가/분석 agent 자체의 할루시네이션 및 정량화 기준의 주관성 | 판정별 결과물 내 근거 위치 인용 의무화(근거 없으면 저신뢰 표기), 질문/판정 검증 agent 이중화, 정량 지표는 개수 기반으로 한정 |
| 리스크 | 임의 생성되는 정규화 schema/tag의 세션 간 일관성 | 세션 내 잠금(생성 후 고정) + 보고서에 schema 명세 동봉. 세션 간 비교는 비목표(NG5)로 처리 |

## 11. 릴리스 계획 (Milestones)

> v0.4: 프론트와 **합동 MVP**로 재정의 — 제품 코어인 deep interview와 다중 agent 협업(drift fan-out)이 M1에서 시연 가능해야 한다는 원칙(GAP_ANALYSIS X-3 해소). 인터뷰는 M1에서 최소형(깊이·질문 수 축소)으로 포함한다.

| 단계 | 범위 | 일정 |
| --- | --- | --- |
| M1 (MVP) | 세션 관리(FR-11), 기획안 업로드/추출(FR-1), **deep interview 최소형**(FR-2~4 — 깊이≤2·질문≤15·조기 종료), 정보 정규화(FR-5), 정량/정성 평가(FR-6), 파일 결과물 sandbox 분석(FR-7 일부), **코어 테마 2종 drift 분석**(FR-8 일부 — 요구 누락·의도 왜곡), 보고서+지표+차트 1종(FR-10, FR-9 일부), 데모 샘플 경로(FR-12), Azure 배포+상태 확인 | |
| M2 | deep interview 확장(FR-2~4 — 깊이 3, SSE, 답변 수정), 웹 페이지·github 분석(FR-7 확장), 시각화 데이터 확장(FR-9), 코어 테마 전체+동적 보조 테마(FR-8 확장), pdf 등 기획안 포맷 확장 검토, 골든셋 기반 정확도 지표 |  |

## 12. 미결 사항 (Open Questions)

> 상세 추적은 [TRD/back.md](TRD/back.md) §13 관리 대장. v0.4 기준 상태:

- [ ] 성공 지표의 목표 수치 (베이스라인 측정 후 설정 — 지표 프레임·전후 효과 주장은 §8로 확정)
- [ ] 동시 세션 규모(G8)의 구체 목표치 (방어 상한은 TRD §8.4로 선확정)
- [ ] pdf 등 추가 기획안 포맷 지원 여부 (M2 검토)
- [x] API endpoint/통신규약 상세 — SCHEMA/schema.md v0.3으로 확정 (SSE·답변 수정만 잔여)
- [x] confused 지표의 산출 산식 상세 — TRD §6.4로 확정 (가중치 캘리브레이션만 잔여)
- [x] 보존 기간 등 개인정보 처리 원칙 — TTL 24h/72h·유저 삭제로 확정 (§7, TRD §10.2)


---

> ## 제2부 — Frontend Design PRD
> 원본: [`PRD/front.md`](PRD/front.md) v0.2 — 이하 원문 전문 수록 (상대 링크만 루트 기준으로 치환, 내용 무변경)

# Frontend Design PRD — mat-copilot

| 항목 | 내용 |
| --- | --- |
| 문서 버전 | v0.2 (Final Draft) |
| 작성자 | @sw1029 |
| 최종 수정일 | 2026-08-22 |
| 변경 요약 | 백엔드 제품 서사와 정합, 기획안 업로드·결과물 제출 화면 신설, SCHEMA v0.3 데이터 계약 반영, 세션·복구·AI 고지 정책 확정 |
| 관련 문서 | [PRD/back.md](PRD/back.md), [TRD/front.md](TRD/front.md), [TRD/back.md](TRD/back.md), [SCHEMA/schema.md](SCHEMA/schema.md), [GAP_ANALYSIS.md](GAP_ANALYSIS.md), [AGENTS.md](AGENTS.md), [regulations.md](regulations.md) |

## 1. 개요 (Overview)
mat-copilot은 AI agent가 기획안 업로드 또는 deep interview로 사용자의 모호한 의도를 구체화하고, 그 의도를 기준선 문서(IntentDoc, Markdown)로 정리한 뒤, 사용자가 제출한 결과물(파일·링크·GitHub)이 의도 대비 얼마나·어느 지점에서 어긋났는지(drift)를 근거 인용과 함께 분석·보고하는 로그인 없는 웹앱이다.

프론트엔드는 React 기반의 전체 화면 고정형 웹 애플리케이션으로 구성한다. 사용자는 홈에서 기획안을 업로드해 인터뷰를 건너뛰거나 단축할 수 있고, 기획안이 없으면 한 번에 하나씩 제시되는 질문에 답하며 의도 기준선을 만든다. 질문 흐름은 백엔드가 반환하는 트리 관계를 따라 마인드맵으로 표현하며, 캔버스만 드래그하여 탐색한다.

핵심 화면 흐름은 `홈/기획안 업로드 또는 첫 질문 → 연속 질의 마인드맵 → 결과물 제출 → 분석 대기 → 분석 결과`다. 프론트 정적 번들은 백엔드 컨테이너(Azure Container Apps)에서 단일 URL로 서빙되어 별도 CORS 구성이 필요 없도록 한다.

## 2. 배경 및 문제 정의 (Background & Problem)
| 구분 | 내용 |
| --- | --- |
| 현재 상황 | 사용자의 초기 의도는 대체로 모호하며, AI에게 문서·코드·기타 산출물 생성을 위임하는 과정에서 요구사항 누락, 의미 변형, 범위 초과, 할루시네이션이 발생할 수 있다. |
| 사용자 문제 | 결과물이 원래 의도와 어디서 어긋났는지 수동으로 대조하려면 문서 10페이지·코드 수백 라인 기준 30~60분 이상이 걸리고, 근거 누락 위험이 크다. |
| UX 문제 | 여러 질문을 한 번에 받으면 답변 부담이 커지고, 긴 분석 대기 중에는 서비스가 멈춘 것으로 오해하기 쉽다. |
| 신뢰 문제 | 단순 점수나 요약만으로는 어떤 결과물 위치가 어떤 의도와 충돌하는지 판단하기 어렵다. |
| 해결 방향 | 기획안 또는 deep interview로 의도 기준선을 확보하고, 결과물 제출 후 수 분 내 근거 인용 보고서와 개선 제안을 제공한다. |

## 3. 목표 및 비목표 (Goals & Non-Goals)
### 3.1 목표
| ID | 목표 |
| --- | --- |
| G1 | 사용자가 로그인 없이 홈에서 기획안 업로드, deep interview, 샘플 체험 중 하나를 바로 시작할 수 있게 한다. |
| G2 | 항상 한 번에 하나의 활성 질문만 제시하여 답변 부담을 낮추고, 질문 흐름은 마인드맵으로 탐색하게 한다. |
| G3 | 백엔드가 반환하는 가변 길이 질문과 분기 구조를 제한 없이 렌더링한다. |
| G4 | 인터뷰 종료 또는 기획안 업로드 후 결과물 제출 화면에서 파일·링크·GitHub를 다건 제출하고 수집 상태를 확인하게 한다. |
| G5 | 분석 진행 상태를 백엔드 JobStage에 맞춰 `의도·결과물 정리 → 차이 분석 → 보고서 생성`으로 투명하게 전달한다. |
| G6 | 결과 화면에서 IntentDoc, 결과물 근거, 테마별 finding, 지표, 개선 제안을 각주와 교차 강조로 연결한다. |
| G7 | 지표는 개수·비율 기반으로 표시하고, 산정 불가 상태와 사유를 명확히 보여준다. |
| G8 | 새로고침·재접속·다중 탭 상황에서도 서버 상태를 기준으로 안전하게 복구한다. |
| G9 | AI가 생성하거나 보조한 모든 화면 표면에 AI 고지 배지 또는 문구를 표시한다. |

### 3.2 비목표
| ID | 비목표 |
| --- | --- |
| NG1 | 프론트엔드에서 LLM 추론, drift 판정, 파일 파싱, 정량 산식을 직접 수행하지 않는다. |
| NG2 | MVP에서 전문 편집기, 실시간 공동 편집, 세션 간 이력 관리, 사용자 계정 기능을 제공하지 않는다. |
| NG3 | MVP에서 사용자가 질문 노드를 임의 생성·삭제하거나 연결선을 직접 수정하는 기능은 제공하지 않는다. |
| NG4 | 결과물을 자동 수정하거나 외부 저장소에 쓰기 작업을 수행하지 않는다. 개선 제안까지만 제공한다. |
| NG5 | 모바일 전용 앱은 만들지 않는다. 단, 결과 화면·보고서 열람 등 핵심 판독 경로는 모바일 반응형을 보장한다. |

## 4. 사용자 및 이해관계자 (Users & Stakeholders)
| 구분 | 설명 | 프론트엔드에 대한 기대 |
| --- | --- | --- |
| 최종 사용자 | 구조화되지 않은 아이디어나 AI 위임 산출물을 검토하려는 사용자 | 적은 학습 비용, 빠른 시작, 의도 대비 결과물 차이의 근거 있는 보고서 |
| 기획자/PM | 기획안·요구사항과 산출물의 정합성을 확인하려는 사용자 | IntentDoc 기준선, 누락·왜곡 위치, 개선 제안 |
| 개발자/테크 리드 | 코드·GitHub 결과물이 의도와 맞는지 확인하려는 사용자 | 파일·라인·링크 근거, 테마별 심각도, 재작업 우선순위 |
| 심사자 | 로그인 없이 짧은 시간 안에 핵심 기능을 평가하는 사용자 | 샘플 체험 경로, 3분 내 완주 가능한 데모, Azure URL 접근성 |
| 백엔드/AI 에이전트 팀 | 세션, 질문, 결과물, 분석 job, 보고서 API를 제공하는 팀 | 명확한 상태·오류·데이터 계약과 프론트 사전 검증 규칙 |

## 5. 정보 구조 및 핵심 사용자 흐름
### 5.1 전역 구조
| 항목 | 요구사항 |
| --- | --- |
| 화면 셸 | 브라우저 뷰포트 전체 크기로 고정하고, 본문 전체 스크롤 대신 화면 내부 영역만 스크롤·드래그한다. |
| 홈 버튼 | 좌측 상단에 모든 단계에서 식별 가능한 홈 버튼을 고정 배치한다. 진행 중 입력 또는 job이 있으면 확인 모달을 표시한다. |
| 세션 복구 | localStorage에 미만료 세션 토큰이 있으면 홈에서 `이어하기` 배너를 표시한다. |
| 서버 우선 | 다중 탭 또는 stale 화면에서는 서버가 반환한 SessionStatus와 최신 트리를 항상 신뢰한다. |
| 데이터 삭제 | 모든 주요 화면에서 접근 가능한 `내 데이터 지우기` 액션을 제공하고 `DELETE /sessions/{id}` 성공 시 토큰과 화면 상태를 즉시 폐기한다. |

### 5.2 기본 흐름
| 단계 | 사용자 행동 | 프론트 동작 | 다음 상태 |
| --- | --- | --- | --- |
| 1 | 홈 진입 | 세션 생성 또는 기존 토큰 복구. 기획안 업로드, 첫 질문 시작, 샘플 체험 CTA를 표시한다. | INITIAL |
| 2-A | 기획안 업로드 | `.docx/.txt/.md` 파일 사전 검증 후 업로드한다. 업로드 성공 시 인터뷰를 건너뛰거나 단축한다. | SUBMITTING |
| 2-B | 첫 질문 답변 | 질문 카드에 답변을 입력하고 제출한다. 다음 질문 수신 전 스켈레톤 노드를 표시한다. | INTERVIEWING |
| 3 | 연속 질의 | 백엔드가 반환한 `nextQuestions[]`를 트리에 추가한다. 조기 종료 버튼을 제공한다. | INTERVIEWING 또는 SUBMITTING |
| 4 | 결과물 제출 | 파일·링크·GitHub를 다건 제출하고 `ingestStatus` 목록을 확인한다. | SUBMITTING |
| 5 | 분석 시작 | `분석 시작` 액션으로 job을 만들고 대기 화면으로 전환한다. | ANALYZING |
| 6 | 분석 대기 | 2초 폴링과 ETag를 사용해 단계 상태를 갱신한다. 취소 버튼을 제공한다. | ANALYZING 또는 FAILED |
| 7 | 결과 확인 | IntentDoc, finding, evidence, metrics, suggestions를 렌더링한다. | COMPLETED |
| 8 | 다운로드 | IntentDoc과 보고서 Markdown 원문을 클라이언트에서 파일로 생성해 내려받는다. | COMPLETED |

### 5.3 상태 흐름
| 프론트 상태 | 백 SessionStatus | 대표 화면 | 설명 |
| --- | --- | --- | --- |
| INITIAL | CREATED | 홈 | 세션은 있으나 의도 확보 전 상태 |
| INTERVIEWING | INTERVIEWING | 마인드맵 | 질문·답변 진행 중 |
| SUBMITTING | INTERVIEW_DONE | 결과물 제출 | 의도 기준선 확보 후 결과물 제출 대기 |
| ANALYZING | ANALYZING | 분석 대기 | 비동기 job 진행 중 |
| COMPLETED | REPORT_READY | 분석 결과 | 보고서 조회 가능 |
| FAILED | 실패 응답 또는 job 실패 | 오류 상태 | 실패 단계와 재시도/취소/새 세션 액션 표시 |
| EXPIRED | EXPIRED 또는 404 | 만료 안내 | 토큰 폐기 후 `새 세션 시작` CTA 표시 |

## 6. 화면별 디자인 요구사항
### 6.1 화면 1 — 홈, 기획안 업로드 및 첫 질문
| 영역 | 요구사항 |
| --- | --- |
| 시작 선택 | `기획안 업로드`, `질문으로 시작`, `샘플로 체험` CTA를 동일 위계로 제공한다. |
| 질문 설정 | `confuseThreshold`와 `timeLimitSec`를 고급 설정으로 제공하되 기본값으로도 바로 시작할 수 있게 한다. |
| 기획안 업로드 | `.docx/.txt/.md`, 10MB 이하만 허용한다. 업로드 성공 시 추출된 의도 요약과 AI 고지 배지를 표시한다. |
| 샘플 체험 | 번들 샘플 기획안과 샘플 결과물을 자동 제출해 심사자가 약 3분 안에 결과 화면까지 도달하는 데모 경로를 제공한다. |
| 첫 질문 | 질문으로 시작할 경우 첫 질문 카드 하나와 답변 입력 영역만 표시한다. |
| 질문 카드 | `prompt`, `helperText`, 필수/선택 여부, AI 생성 배지, 답변 입력 필드, 제출 버튼을 포함한다. |
| 입력 검증 | 답변은 2,000자 이하, 공백만 입력 불가로 사전 검증한다. |
| rate limit | 세션 생성이 429로 실패하면 `Retry-After` 기반 카운트다운과 재시도 버튼을 표시한다. |

### 6.2 화면 2 — 연속 질의 마인드맵
| 항목 | 요구사항 |
| --- | --- |
| 노드 배치 | 질문 노드는 좌측에서 우측 방향으로 시간 순서 또는 `parentId` 관계에 따라 배치한다. 병합은 표현하지 않는다. |
| 노드 상태 | 완료, 활성, 전송 중, 오류, 선택 상태를 색상·아이콘·텍스트로 함께 표시한다. |
| AI 고지 | `aiGenerated:true` 질문에는 `AI 생성 질문` 배지를 표시한다. |
| 활성 질문 | 한 번에 하나의 노드만 입력 가능하다. 아직 도착하지 않은 질문은 미리 표시하지 않는다. |
| 스켈레톤 | 답변 제출 직후 흐름 끝에 스켈레톤 노드를 표시하고 중복 제출을 막는다. 30초가 지나면 `질문을 정리하는 중입니다. 잠시만 기다려 주세요.` 안내를 추가한다. |
| 남은 질문 | `remainingQuestions`는 잔여 질문 예산 힌트로만 표시하며 정확한 총량처럼 보이지 않게 한다. |
| 조기 종료 | `그만 답하고 넘어가기` 버튼을 제공한다. REQUIRED 미답변이 있으면 목록과 경고 모달을 표시하고, 사용자가 확인하면 강행한다. |
| 캔버스 | 질문 캔버스 또는 핸들만 드래그 가능하며 페이지와 전역 헤더는 고정한다. 입력 필드 조작과 드래그가 충돌하지 않아야 한다. |
| 현재 질문 복귀 | `현재 질문으로 이동` 버튼을 제공하고, 노드를 영구히 잃지 않도록 이동 범위 또는 위치 복원 기능을 제공한다. |
| 접근성 | 키보드로 노드 간 이동, 활성 질문 포커스, 신규 노드 aria-live 알림을 제공한다. 모바일·보조기술 환경에서는 리스트 뷰 폴백을 제공한다. |
| 답변 수정 | MVP에서는 제공하지 않는다. M2에서 도입 시 이후 분기 무효화 안내와 재생성 계약을 따른다. |

### 6.3 화면 3 — 결과물 제출
| 영역 | 요구사항 |
| --- | --- |
| 진입 조건 | 기획안 업로드 완료, 인터뷰 완료, 또는 조기 종료 완료 후 진입한다. |
| 제출 유형 | 파일, 일반 HTTPS 링크, GitHub URL을 다건 제출할 수 있다. |
| 파일 한도 | 결과물 파일은 20MB 이하, 세션당 20건 이하로 사전 검증한다. zip은 해제 후 100MB·1,000개 파일 초과 가능성을 안내하고 백엔드 차단 상태를 표시한다. |
| URL 한도 | 링크는 HTTPS만 허용한다. GitHub 유형은 `github.com` 도메인만 허용한다. |
| 제출 목록 | 각 결과물을 `name`, `type`, `ingestStatus`, `ingestNote`, `submittedAt`와 함께 목록으로 표시한다. |
| 상태 표시 | `PENDING`, `PARSED`, `SKIPPED_UNSUPPORTED`, `SKIPPED_TOO_LARGE`, `BLOCKED_UNSAFE`를 사용자 친화 문구와 아이콘으로 표시한다. |
| 분석 시작 | 최소 1개 이상 제출된 결과물이 있으면 `분석 시작` 버튼을 활성화한다. 차단·제외된 항목은 분석 제외 가능성을 명확히 알린다. |
| 삭제 | 분석 시작 전 제출 항목 제거와 세션 전체 삭제를 구분해 제공한다. |

### 6.4 화면 4 — 분석 대기
| 항목 | 요구사항 |
| --- | --- |
| 단계 라벨 | ① 의도·결과물 정리 ② 차이 분석 ③ 보고서 생성 순서로 표시한다. |
| JobStage 매핑 | ① `INGEST/NORMALIZE/EVALUATE`, ② `DRIFT`, ③ `AGGREGATE/REPORT`로 그룹화한다. |
| 진행률 | `progress`가 숫자로 제공될 때만 백분율을 표시한다. `null`이면 임의 값을 만들지 않고 단계형 로더를 사용한다. |
| 폴링 | 2초 간격으로 job 상태를 조회하고 ETag/If-None-Match를 사용한다. 304 응답은 기존 표시를 유지한다. SSE는 MVP 범위가 아니다. |
| 취소 | `취소` 버튼을 제공한다. 선택 시 진행 중 fetch를 abort하고 `POST /jobs/{jobId}/cancel`을 호출한다. |
| 실패 | 실패 단계, 이해 가능한 메시지, 재시도 또는 새 세션 시작 액션을 표시한다. 완료된 이전 단계는 반복 실행하지 않는다. |
| 복구 | 새로고침 또는 재접속 시 `activeJobId` 기준으로 폴링을 재개한다. |

### 6.5 화면 5 — 분석 결과
#### 전체 레이아웃
| 항목 | 요구사항 |
| --- | --- |
| 데스크톱 | 좌측 30%, 우측 70%의 2열 구조를 기본으로 한다. 우측은 상부 지표/차트와 하부 상세 리포트로 나눈다. |
| 모바일 | 핵심 판독 경로를 보장하기 위해 IntentDoc, 지표, finding 목록, 상세 리포트를 세로 탭 또는 아코디언으로 전환한다. |
| 스크롤 | 각 패널은 고정된 전체 화면 안에서 독립 스크롤한다. |
| AI 고지 | IntentDoc, 보고서 본문, 개선 제안 영역에 AI 생성/보조 고지 배지를 표시한다. |

#### 좌측 30% — IntentDoc 및 결과물 근거 패널
| 영역 | 요구사항 |
| --- | --- |
| IntentDoc | 백엔드가 제공한 `intentDoc.markdown`을 렌더링하고 블록별 `blockId` 앵커를 유지한다. |
| 결과물 근거 | 선택한 finding의 `evidence[]`를 파일·웹·GitHub 위치별로 표시한다. 경로, 라인, URL, 인용문을 가능한 범위에서 보여준다. |
| 각주 | 각주 번호는 프론트가 화면 렌더링 시 파생한다. 동일 finding 내 IntentDoc 블록과 evidence를 같은 번호군으로 연결한다. |
| 누락 표현 | 결과물에 근거가 없는 누락 finding은 `결과물에서 대응 근거를 찾지 못함`으로 표시하며 오류처럼 취급하지 않는다. |
| 교차 강조 | 각주나 finding 선택 시 IntentDoc 블록, 결과물 근거, 차트, 상세 리포트를 동기화한다. 위치 이동이 불가하면 `위치 이동 불가` 배지를 표시한다. |
| 다운로드 | IntentDoc Markdown 원문은 클라이언트에서 `.md` 파일로 생성해 내려받는다. |

#### 우측 70% / 상부 — 지표 및 차트
| 항목 | 요구사항 |
| --- | --- |
| 지표 유형 | 의도 커버리지, 테마별 finding 건수, 심각도 분포, 산출물 수집 상태, 토큰 사용량 참고 등 개수·비율 기반 지표를 우선 제공한다. |
| 점수 정책 | 주관적 총점은 제공하지 않는다. 심각도와 신뢰도는 백엔드가 제공한 등급 라벨로 표시한다. |
| 메타 사용 | 임계값, 상태, 설명은 `metrics[]`의 메타를 사용하고 프론트에 하드코딩하지 않는다. |
| 산정 불가 | `computable=false`인 지표는 `산정 불가`와 `reason`을 표시하며 0이나 실패로 오인되지 않게 한다. |
| 차트 | M1은 차트 1종 이상을 제공한다. 항목 선택 시 관련 finding과 좌측 근거를 함께 활성화한다. |

#### 우측 70% / 하부 — 상세 리포트
| 항목 | 요구사항 |
| --- | --- |
| 기본 상태 | finding 선택 방법과 전체 분석 요약을 안내한다. |
| 선택 상태 | `theme`, `summary`, `detail`, `severity`, `confidence`, `evidence`, `suggestion`, `intentBlockIds`를 표시한다. |
| 근거 인용 | evidence의 quote를 원문 인용으로 표시하고 위치 링크를 제공한다. |
| 개선 제안 | 제안이 AI 보조 산출물임을 명시하고, 자동 수정이 아닌 사용자가 검토할 다음 행동으로 표현한다. |
| 조기 종료 | `earlyCompleted`가 있으면 일부 의도가 덜 구체화되었을 수 있음을 보고서 상단에 표시한다. |
| 보고서 다운로드 | `qualitative(markdown)` 및 필요한 요약을 클라이언트에서 `.md` 파일로 생성해 내려받는다. |

## 7. 핵심 사용자 스토리 (User Stories)
| ID | 사용자 스토리 | 우선순위 |
| --- | --- | --- |
| US-1 | 사용자로서 기획안을 업로드해 초기 의도를 빠르게 추출하고 싶다. 처음부터 다시 설명하는 시간을 줄이고 싶기 때문이다. | P0 |
| US-2 | 사용자로서 기획안이 없을 때 질문을 하나씩 받으며 의도를 구체화하고 싶다. 모호한 생각을 부담 없이 정리하고 싶기 때문이다. | P0 |
| US-3 | 사용자로서 질문 흐름을 마인드맵으로 탐색하고 싶다. 현재 질문이 나온 맥락을 이해하고 싶기 때문이다. | P0 |
| US-4 | 사용자로서 충분히 답했다고 판단하면 조기 종료하고 다음 단계로 넘어가고 싶다. 해커톤 데모와 실제 사용 시간을 통제하고 싶기 때문이다. | P0 |
| US-5 | 사용자로서 파일·링크·GitHub 결과물을 여러 개 제출하고 수집 상태를 확인하고 싶다. 분석 누락 원인을 미리 알고 싶기 때문이다. | P0 |
| US-6 | 사용자로서 분석이 어느 단계까지 진행됐는지 알고 필요하면 취소하고 싶다. 대기 중 통제감을 갖고 싶기 때문이다. | P0 |
| US-7 | 사용자로서 IntentDoc과 결과물 근거를 각주로 연결해 보고 싶다. 어떤 의도와 어떤 결과 위치가 충돌하는지 빠르게 확인하고 싶기 때문이다. | P0 |
| US-8 | 사용자로서 지표와 상세 리포트에서 drift의 원인, 영향 및 개선 제안을 확인하고 싶다. 다음 작업에 바로 반영하고 싶기 때문이다. | P0 |
| US-9 | 사용자로서 IntentDoc과 보고서를 Markdown 파일로 내려받고 싶다. 외부 업무 흐름에서 활용하고 싶기 때문이다. | P1 |
| US-10 | 키보드 또는 보조기술 사용자로서 마우스 드래그 없이도 핵심 기능을 이용하고 싶다. | P1 |

## 8. 기능 요구사항 (Functional Requirements)
| ID | 기능 | 설명 | 우선순위 |
| --- | --- | --- | --- |
| FR-1 | 전체 화면 셸 | 뷰포트 고정 레이아웃, 홈 버튼, 내부 스크롤·드래그 영역, 세션 삭제 진입점을 제공한다. | P0 |
| FR-2 | 시작 경로 | 홈에서 기획안 업로드, 질문으로 시작, 샘플 체험 경로를 제공한다. | P0 |
| FR-3 | 기획안 업로드 검증 | `.docx/.txt/.md` 10MB 이하 조건을 사전 검증하고 업로드 상태를 표시한다. | P0 |
| FR-4 | 첫 질문 표시 | 질문 시작 시 첫 질문과 답변 입력 UI만 표시한다. | P0 |
| FR-5 | 순차 답변 제출 | 답변 유효성을 검사하고 중복 제출 없이 백엔드에 전달한다. | P0 |
| FR-6 | 동적 질문 렌더링 | 백엔드가 반환한 질문 노드와 부모 관계에 따라 마인드맵을 동적으로 추가한다. | P0 |
| FR-7 | 활성 질문 제어 | 한 번에 하나의 질문만 입력 가능 상태로 유지하고 최신 서버 트리를 신뢰한다. | P0 |
| FR-8 | 챗봇 캔버스 드래그 | 페이지가 아닌 질문 캔버스만 포인터로 이동할 수 있게 한다. | P0 |
| FR-9 | 현재 질문 복귀 | 캔버스에서 활성 질문 위치로 즉시 돌아오는 기능을 제공한다. | P0 |
| FR-10 | 조기 종료 | `그만 답하고 넘어가기` 버튼, REQUIRED 미답변 경고, 강행 확인 모달을 제공한다. | P0 |
| FR-11 | 결과물 제출 | 파일·링크·GitHub 다건 제출, 한도 검증, 수집 상태 목록, 분석 시작 액션을 제공한다. | P0 |
| FR-12 | 작업 상태 표시 | 백엔드 JobStage를 3단계 라벨로 매핑해 진행·실패·완료 상태를 표시한다. | P0 |
| FR-13 | 분석 취소 | 진행 중 fetch abort와 job 취소 API 호출을 제공한다. | P0 |
| FR-14 | 세션 복구 | localStorage 토큰으로 새로고침·재접속 후 상태별 화면을 복원한다. | P0 |
| FR-15 | 결과 분할 레이아웃 | 결과를 데스크톱 좌측 30%, 우측 70% 구조로 제공하고 모바일 핵심 판독 레이아웃을 제공한다. | P0 |
| FR-16 | IntentDoc 렌더링 | IntentDoc Markdown과 blockId 앵커를 렌더링한다. | P0 |
| FR-17 | 근거 각주 표시 | 프론트 파생 각주 번호로 IntentDoc 블록과 evidence 위치를 연결한다. | P0 |
| FR-18 | 교차 강조 | 각주·지표·finding 선택 시 IntentDoc, 결과물 근거, 차트, 상세 리포트를 동기화한다. | P0 |
| FR-19 | 분석 시각화 | 개수·비율 기반 metrics와 차트 1종 이상을 표시한다. | P0 |
| FR-20 | 상세 리포트 | finding의 요약, 상세, 근거 인용, 심각도, 신뢰도, 개선 제안을 표시한다. | P0 |
| FR-21 | AI 고지 | 질문, IntentDoc, 보고서, 개선 제안 등 AI 생성 표면 전부에 고지를 표시한다. | P0 |
| FR-22 | 오류 및 재시도 | 질문 제출, 업로드, 결과물 제출, 분석 실패를 단계별로 안내하고 재시도·새 세션 시작을 제공한다. | P0 |
| FR-23 | 클라이언트 다운로드 | 계약에 포함된 IntentDoc·보고서 Markdown 원문을 브라우저에서 `.md` 파일로 생성한다. | P1 |
| FR-24 | 결과 빈 상태 | finding, evidence, metrics가 비어 있거나 산정 불가인 경우 원인과 가능한 다음 행동을 안내한다. | P1 |
| FR-25 | 이탈 방지 | 미제출 답변, 업로드 중 파일, 진행 중 분석이 있을 때 홈 이동 전 확인한다. | P1 |

### 8.1 프론트엔드 데이터 계약 개요
상세 엔드포인트와 스키마는 SCHEMA v0.3을 따른다. 프론트엔드는 아래 필드를 화면 계약으로 사용한다.

| 데이터 | 필수 필드 | 프론트 사용 목적 |
| --- | --- | --- |
| Session | `sessionId`, `status`, `expiresAt`, `activeJobId?`, `interviewStartedAt?` | 상태 라우팅, TTL 표시, job 폴링 재개 |
| SessionStatus 매핑 | `CREATED`, `INTERVIEWING`, `INTERVIEW_DONE`, `ANALYZING`, `REPORT_READY`, `EXPIRED` | INITIAL, INTERVIEWING, SUBMITTING, ANALYZING, COMPLETED, EXPIRED 화면 전환 |
| QuestionNode | `questionId`, `parentId`, `depth`, `prompt`, `helperText?`, `kind`, `status`, `confused?`, `intentPhase`, `inputType:"text"`, `aiGenerated:true`, `createdAt` | 질문 카드, 트리 렌더링, AI 고지, 필수 질문 경고 |
| Answer submit response | `nextQuestions[]`, `remainingQuestions`, `interviewStatus`, `completedReason?` | 다음 노드 추가, 잔여 질문 힌트, 제출 화면 전환, 종료 사유 표시 |
| Early complete | `POST /interview/complete`, `confirm=true`, REQUIRED 미답변 목록 | 조기 종료 버튼과 경고 모달 |
| Artifact | `artifactId`, `type`, `name`, `url?`, `ingestStatus`, `ingestNote?`, `submittedAt` | 결과물 제출 목록과 수집 상태 표시 |
| Job status | `jobId`, `stage`, `status`, `progress?`, `error?`, `etag` | 3단계 대기 화면, 2초 폴링, ETag 캐시, 실패 재시도 |
| Job cancel | `POST /jobs/{jobId}/cancel` | 분석 취소 버튼과 fetch abort 처리 |
| Report | `metrics[]`, `quantStats`, `qualitative(markdown)`, `suggestions[]`, `findings[]`, `intentDoc`, `normalizationSchema`, `aiGeneratedNotice`, `earlyCompleted?` | 결과 화면 전체 데이터 |
| IntentDoc | `markdown`, `blocks[{blockId:"ib-<seq>", intentIds[]}]` | 좌측 문서 패널, blockId 앵커, 교차 강조 |
| Finding | `theme`, `summary`, `detail`, `evidence[]`, `severity`, `confidence`, `suggestion?`, `intentBlockIds[]` | 상세 리포트, finding 목록, IntentDoc 연결 |
| EvidenceRef | `artifactId`, `location{kind,path?,startLine?,endLine?,url?,note?}`, `quote` | 결과물 위치 표시, 인용문, 근거 이동 |
| Metric | `metricId`, `label`, `value`, `unit`, `thresholds?`, `status`, `description`, `computable`, `reason?` | 지표 카드, 차트, 산정 불가 표시 |

### 8.2 프론트 사전 검증 상수
| 항목 | 값 | 실패 시 동작 |
| --- | --- | --- |
| 기획안 파일 | `.docx/.txt/.md`, 10MB 이하 | 업로드 전 차단 및 허용 형식 안내 |
| 결과물 파일 | 파일당 20MB 이하, 세션당 20건 이하 | 제출 전 차단 및 초과 항목 표시 |
| zip 안전 한도 | 해제 후 100MB 이하, 1,000개 이하 | 초과 가능성을 안내하고 백엔드 차단 상태를 표시 |
| 답변 | 2,000자 이하, 공백만 입력 불가 | 제출 버튼 비활성화 및 사유 표시 |
| 일반 URL | HTTPS만 허용 | 입력 단계에서 차단 |
| GitHub URL | `github.com`만 허용 | 입력 단계에서 차단 |
| confuseThreshold | 0~1, step 0.05 | 범위 밖 값 저장 차단 |
| timeLimitSec | 60~3600초 또는 미설정 | 범위 밖 값 저장 차단 |

### 8.3 주요 오류 상태
| 상황 | 사용자 안내 및 동작 |
| --- | --- |
| 세션 생성 rate limit | 429와 `Retry-After`를 기준으로 카운트다운을 표시하고 만료 후 재시도한다. |
| 첫 질문 조회 실패 | 오류 원인과 `다시 불러오기`를 표시한다. |
| 답변 제출 실패 | 입력값을 보존하고 동일 답변을 재시도할 수 있게 한다. |
| 다음 질문 생성 지연 | 스켈레톤 상태를 유지하고 30초 이후 안심 문구를 표시한다. |
| REQUIRED 미답변 조기 종료 | 미답변 목록을 모달에 표시하고 `그래도 넘어가기` 확인을 받는다. |
| 결과물 제출 실패 | 실패 항목만 재시도 가능하게 하며 성공 항목은 유지한다. |
| 분석 취소 | 취소 완료 후 결과물 제출 화면으로 돌아가거나 새 세션을 시작할 수 있게 한다. |
| SESSION_EXPIRED / SESSION_NOT_FOUND | localStorage 토큰을 폐기하고 `새 세션 시작` CTA를 표시한다. |
| 분석 실패 | 완료 단계는 보존하고 실패 단계부터 재시도한다. |
| 결과 일부 누락 | 사용 가능한 결과는 표시하되 누락 영역과 원인을 명시한다. |
| 연결 끊김 | 오프라인 상태를 알리고 연결 복구 후 서버 상태를 재조회한다. |

### 8.4 재접속 복구 라우팅
| 서버 상태 | 복구 화면 | 후속 호출 |
| --- | --- | --- |
| CREATED | 홈 | 필요 시 첫 질문 또는 업로드 상태 조회 |
| INTERVIEWING | 마인드맵 | 질문 트리 전체 재조회 후 활성 노드 복원 |
| INTERVIEW_DONE | 결과물 제출 | 제출 목록 재조회 |
| ANALYZING | 분석 대기 | `activeJobId` 기준 폴링 재개 |
| REPORT_READY | 분석 결과 | 보고서 조회 |
| EXPIRED / 404 | 만료 안내 | 토큰 폐기 후 새 세션 시작 |

## 9. 비기능 요구사항 (Non-Functional Requirements)
| 항목 | 기준 |
| --- | --- |
| 반응성 | 사용자 입력 및 드래그 피드백은 일반적인 데스크톱 환경에서 100ms 이내 반영을 목표로 한다. |
| 렌더링 성능 | 질문 노드 증가 시 드래그 중 체감 프레임 저하를 최소화하며, 필요 시 뷰포트 밖 노드를 가상화한다. |
| 초기 성능 | 주요 화면 LCP 목표는 정상 네트워크 환경에서 2.5초 이내로 한다. |
| 안정성 | 중복 답변 제출, 오래된 응답에 의한 활성 질문 역전, 새로고침에 의한 작업 유실을 방지한다. |
| 접근성 | WCAG 2.1 AA 수준을 목표로 하며 키보드 탐색, 포커스 표시, 스크린 리더 라벨, live region, 충분한 명도 대비를 제공한다. |
| 모바일 | 전면 모바일 최적화는 비목표이나 결과 화면·보고서 열람·세션 삭제 등 핵심 판독 경로는 모바일에서도 사용 가능해야 한다. 마인드맵은 데스크톱 권장, 모바일은 리스트 뷰 폴백을 제공한다. |
| 색상 인지 | 초록·빨강 색상 외에도 텍스트, 아이콘, 형태를 사용해 상태를 구분한다. |
| 보안 | 사용자 입력과 Markdown 출력은 XSS 방지를 위해 sanitize한다. 세션 토큰은 복구 요구에 맞춰 localStorage에 보관하되 만료·삭제 시 즉시 폐기한다. |
| 호환성 | 최신 Chrome, Edge, Firefox의 최근 2개 주요 버전을 지원한다. |
| 해상도 | 데스크톱 1440×900을 기준으로 설계하며 최소 1280×720에서 핵심 기능을 유지한다. |
| 관측성 | 화면 전환, 제출 실패, 작업 실패, 취소, 재시도, 결과 항목 선택, 렌더링 오류를 추적할 수 있어야 한다. |
| 국제화 | MVP는 한국어를 기본으로 하되 UI 문자열을 코드와 분리하여 향후 다국어 확장이 가능해야 한다. |

## 10. 성공 지표 (Success Metrics)
| 지표 | 기준선 | MVP 목표 |
| --- | --- | --- |
| 심사용 샘플 완주 시간 | 신규 | 홈 진입 후 결과 화면 도달까지 3분 이내 |
| 수동 대조 대비 시간 절감 | 문서 10페이지·코드 수백 라인 기준 30~60분+누락 위험 | 제출 후 수 분 내 근거 인용 보고서 제공, 리허설 실측 1회 기록 |
| 첫 행동 시작률 | 측정 필요 | 홈 진입 세션의 80% 이상 |
| 인터뷰 또는 기획안 확보 완료율 | 측정 필요 | 시작 세션의 70% 이상 |
| 결과물 제출 완료율 | 측정 필요 | 의도 확보 세션의 80% 이상 |
| 분석 완료율 | 측정 필요 | 분석 시작 세션의 95% 이상 |
| 결과 화면 도달 후 finding 확인율 | 측정 필요 | 결과 세션의 60% 이상 |
| 특정 finding의 상세 리포트 탐색 시간 | 측정 필요 | 중앙값 10초 이내 |
| 새로고침 후 작업 복구 성공률 | 측정 필요 | 99% 이상 |
| 키보드 핵심 흐름 완료율 | 측정 필요 | 주요 시나리오 100% |
| 프론트엔드 치명적 오류율 | 측정 필요 | 사용자 세션의 0.5% 미만 |

## 11. 제약 및 가정 (Constraints & Assumptions)
### 11.1 제약
| ID | 제약 |
| --- | --- |
| CON-1 | React 기반 웹 애플리케이션으로 구현한다. |
| CON-2 | Copilot SDK와 Microsoft Agent Framework는 백엔드 agent 로직의 필수 구성요소이며, 프론트는 해당 결과를 표시하는 클라이언트다. |
| CON-3 | 최종 산출물은 Azure 클라우드에 배포된 웹앱이어야 한다. 프론트 정적 번들은 백엔드 컨테이너에서 단일 URL로 서빙한다. |
| CON-4 | 로그인 없이 동작해야 하며, 세션 토큰만으로 핵심 흐름을 식별한다. |
| CON-5 | 세션 TTL은 마지막 활동 기준 24시간이며, 보고서 도달 시 72시간으로 연장한다. |
| CON-6 | 사용자는 `내 데이터 지우기`로 세션과 제출 데이터를 즉시 파기할 수 있어야 한다. |
| CON-7 | 질문 트리는 분기만 표현하며 병합은 표현하지 않는다. |
| CON-8 | 결과 화면의 데스크톱 기본 비율은 좌측 30%, 우측 70%다. |
| CON-9 | 프론트는 metric 임계값·상태 기준을 하드코딩하지 않는다. |
| CON-10 | 세션 생성은 IP당 분당 5회로 제한되며, 429 응답 시 카운트다운 UI를 제공한다. |

### 11.2 가정
| ID | 가정 |
| --- | --- |
| A-1 | 백엔드는 각 질문의 고유 ID, 부모 관계, 필수 여부, AI 생성 여부, 종료 신호를 일관된 형식으로 제공한다. |
| A-2 | 기획안 업로드가 성공하면 백엔드는 IntentDoc 생성에 필요한 의도 정보를 확보하거나 인터뷰 단축 상태를 제공한다. |
| A-3 | 분석 결과에는 IntentDoc blockId와 finding의 `intentBlockIds[]`, evidence 위치 정보가 포함된다. |
| A-4 | 분석 지표에는 값뿐 아니라 상태 기준, 단위, 설명, 산정 가능 여부와 사유가 포함된다. |
| A-5 | 분석 작업은 비동기로 수행되며 job ID와 ETag로 상태를 재조회할 수 있다. |
| A-6 | 완료된 보고서는 TTL 내 동일 세션 토큰으로 다시 접근할 수 있다. |

## 12. 의존성 및 리스크 (Dependencies & Risks)
| 구분 | 항목 | 내용 | 대응 방안 |
| --- | --- | --- | --- |
| 의존성 | 세션 API | 로그인 없는 토큰 발급, TTL, 삭제, 상태 복구가 UI 흐름을 결정한다. | 상태 enum과 복구 라우팅을 계약 테스트로 검증한다. |
| 의존성 | 질문 API | 질문 순서·분기·종료 신호가 마인드맵을 결정한다. | `QuestionNode`와 답변 응답 계약을 SCHEMA v0.3에 고정한다. |
| 의존성 | 결과물 API | 수집 상태와 분석 시작 가능 여부가 제출 화면을 결정한다. | `Artifact.ingestStatus`별 사용자 문구를 정의한다. |
| 의존성 | 비동기 job API | 대기 화면, 취소, 재시도, 복구가 job 상태에 의존한다. | 2초 폴링, ETag, 취소 API, 실패 단계 보존을 적용한다. |
| 의존성 | 보고서 계약 | IntentDoc·finding·evidence·metrics가 결과 화면 구현의 핵심이다. | blockId와 구조화 location 기반으로 렌더링한다. |
| 리스크 | 대규모 질문 그래프 | 노드 증가 시 렌더링과 드래그 성능이 저하될 수 있다. | 가상화, 레이아웃 캐시, 리스트 뷰 폴백을 적용한다. |
| 리스크 | 긴 결과물 근거 | 큰 파일·웹 페이지 근거 렌더링이 느려질 수 있다. | evidence 중심 발췌 렌더링과 지연 로딩을 적용한다. |
| 리스크 | 위치 매핑 불안정 | Markdown 재렌더링 또는 외부 링크 변화로 위치 이동이 실패할 수 있다. | 각주는 유지하고 `위치 이동 불가` 상태로 강등한다. |
| 리스크 | 장시간 대기 | 사용자가 작업을 중단하거나 중복 실행할 수 있다. | 단계형 진행 표시, 취소, 상태 복구, 중복 작업 방지를 제공한다. |
| 리스크 | 분석 신뢰성 오해 | 사용자가 AI 판단을 절대적 평가로 받아들일 수 있다. | AI 고지, 근거 인용, confidence, 산정 방식 설명을 함께 제공한다. |
| 리스크 | 공개 무로그인 남용 | 익명 세션 생성과 LLM 호출 비용이 증가할 수 있다. | rate limit, 세션 TTL, 제출 한도, 삭제 기능을 적용한다. |

## 13. 릴리스 계획 (Milestones)
| 단계 | 범위 | 완료 조건 |
| --- | --- | --- |
| M1 — 합동 MVP | 세션 관리, 기획안 업로드/추출, 인터뷰 최소형(깊이≤2, 질문≤15, 조기 종료), 결과물 제출, 파일 분석, drift 코어 2종, IntentDoc, 보고서(metrics·차트 1종), Azure 배포 | 로그인 없이 샘플 또는 사용자 입력으로 홈부터 결과 화면까지 완주할 수 있음 |
| M2 — 분석 확장 | 웹/깃헙 수집, 코어 4종+동적 테마, SSE 검토, 답변 수정, PDF 지원, 고급 차트 | 다양한 결과물 유형과 복잡한 질문 흐름을 안정적으로 처리함 |
| M3 — 품질 개선 | 접근성 세부 개선, 성능 최적화, 관측성 대시보드, 빈 상태 및 세부 UX 개선 | 지원 브라우저·해상도·모바일 핵심 판독 경로에서 품질 기준 충족 |

## 14. 수용 기준 (Acceptance Criteria)
| ID | 기준 |
| --- | --- |
| AC-1 | 홈에서 `기획안 업로드`, `질문으로 시작`, `샘플로 체험` 경로가 모두 보인다. |
| AC-2 | 샘플 체험은 로그인 없이 결과 화면까지 완주할 수 있다. |
| AC-3 | 첫 질문 시작 시 질문 하나와 답변 입력 요소만 표시된다. |
| AC-4 | 답변 제출 후 백엔드가 반환한 다음 질문이 기존 질문 오른쪽에 연결되어 표시된다. |
| AC-5 | 질문 개수가 사전에 정해지지 않아도 모든 반환 질문을 렌더링할 수 있다. |
| AC-6 | 사용자는 페이지 전체가 아닌 챗봇 캔버스만 드래그할 수 있다. |
| AC-7 | 답변 제출 후 스켈레톤 노드가 보이고 30초 경과 시 안심 문구가 표시된다. |
| AC-8 | 조기 종료 시 REQUIRED 미답변 목록과 강행 확인 모달이 표시된다. |
| AC-9 | 인터뷰 완료 또는 기획안 업로드 후 결과물 제출 화면으로 이동한다. |
| AC-10 | 결과물 제출 화면은 파일·HTTPS 링크·GitHub URL 다건 제출과 수집 상태 목록을 제공한다. |
| AC-11 | 분석 대기 화면은 3단계 라벨을 표시하고 `progress`가 없으면 단계형 로더를 사용한다. |
| AC-12 | 분석 대기 중 취소 버튼이 동작하며 완료 후 제출 화면 또는 새 세션 시작으로 이동할 수 있다. |
| AC-13 | 새로고침 후에도 서버 상태에 따라 홈, 마인드맵, 제출, 대기, 결과 화면 중 적절한 화면으로 복구된다. |
| AC-14 | 결과 화면은 데스크톱에서 좌측 30%, 우측 70% 구조를 유지한다. |
| AC-15 | 모바일에서 결과 화면과 보고서 열람이 세로 레이아웃으로 가능하다. |
| AC-16 | IntentDoc blockId와 finding evidence가 프론트 파생 각주로 연결된다. |
| AC-17 | 각주 선택 시 문서 블록, 결과물 근거, 시각화, 상세 리포트가 동기화된다. |
| AC-18 | metric이 산정 불가이면 `산정 불가`와 사유가 표시된다. |
| AC-19 | 질문, IntentDoc, 보고서, 개선 제안에는 AI 생성 또는 보조 고지가 표시된다. |
| AC-20 | 실패한 질문 제출 또는 결과물 제출 시 사용자의 입력값이 유실되지 않는다. |
| AC-21 | SESSION_EXPIRED 또는 SESSION_NOT_FOUND 발생 시 토큰을 폐기하고 `새 세션 시작` CTA를 표시한다. |
| AC-22 | `내 데이터 지우기` 성공 후 localStorage 토큰과 화면 상태가 제거된다. |
| AC-23 | 키보드만으로 시작, 질문 답변, 제출, 결과 상세 탐색까지 핵심 흐름을 수행할 수 있다. |

## 15. 미결 사항 (Open Questions)
아래 항목은 v0.1에서 미결이었으나 v0.2에서 제품 단일화와 함께 종결한다.

| 항목 | v0.2 결정 | 후속 |
| --- | --- | --- |
| 질문 입력 유형 | MVP는 `text` 고정. 확장을 위해 `inputType:"text"` 필드는 유지한다. | M2에서 선택형·파일형 검토 |
| 질문 그래프 | 분기만 존재하고 병합은 없다. 각 노드는 단일 `parentId`를 가진다. | 복잡한 그래프는 비목표 |
| 이전 답변 수정 | MVP 보류. | M2에서 subtree 무효화와 재생성 UX 검토 |
| 상태 전달 방식 | 2초 폴링 + ETag/If-None-Match를 사용한다. | 실측 후 SSE 재검토 |
| 지표 산식 | 프론트는 산식을 보유하지 않고 백엔드 `metrics[]` 메타를 사용한다. | TRD/SCHEMA 계약 테스트 |
| 각주 위치 | IntentDoc `blockId`와 구조화된 evidence location을 사용하며 각주 번호는 프론트가 파생한다. | 렌더링 안정성 테스트 |
| 인증·보존 | 로그인 없음, localStorage 세션 토큰, TTL 24h/보고서 72h, 유저 주도 삭제를 사용한다. | 개인정보 문구와 삭제 API 테스트 |
| 모바일 범위 | 전면 대응은 비목표이나 결과 화면·보고서 핵심 판독 경로는 보장한다. | 리스트 뷰와 세로 결과 레이아웃 구현 |
